# Events

`events.jsonl` is an append-only machine-readable event stream. Each non-empty line must be a JSON object with a timestamp, event type, actor Agent ID or `human`, and references to affected work items or evidence.
