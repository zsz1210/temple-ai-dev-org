# Doctor compiler reuse and memory evidence

Maintainer authorization: proceed with the previously recommended Doctor optimization after the Node/Rust discussion, and investigate high RAM usage. Baseline main is `d3892f6b018c006c28d6cf13590548fed9561f53`. This is a standard-risk implementation optimization, not a change to schema acceptance policy or verification gates. Developer Rikku; distinct QA Lulu; Principal human.

Scope: `src/schema-validation.mjs`, a focused behavioral test file, and this item's evidence. Reuse an Ajv base environment only within one schema-validation call for explicit draft-2020-12 schemas whose references remain local and which have no nested schema identities. Remove user schemas between compilations so duplicate IDs and cross-entry references cannot acquire implicit authority. Other schema forms keep fresh isolated compilers. Continue reading every current schema and document, applying formats, meta-schema validation, semantic checks and learning validation; never cache verdicts, schema files, claims or repository state. Do not change Node, dependencies, CLI protocol, existing tests, test concurrency defaults, initialization fixture semantics or the approved verification commands.

Acceptance: cover duplicate IDs with different content; external and nested reference isolation; unusual/default dialect behavior; failed compilation followed by valid compilation; between-call schema/document changes, separate roots and simultaneous calls. Compare against the retained baseline on fixed fresh processes, including valid and invalid results, elapsed time and process memory. Record memory as process RSS/heap/peak with its limits, not as total application footprint. Snapshot the workstation without reading private content, printing arguments or stopping unrelated apps. During the single final full verification, sample only descendants of that owned run; do not infer a leak from one high RSS snapshot.

Verification: focused tests and existing installation integration; bounded alternating baseline/candidate measurements (four fresh samples each using identical synthetic input); one final `npm run verify` on the accepted behavioral candidate; distinct source/adversarial QA; fresh Doctor/Status and evidence-only fast checks. No duplicate full run without a changed candidate or new unresolved concern. Preserve raw logs and measurement scripts. Full-suite monitoring is passive; existing concurrency and scope remain unchanged.

Stop: complete local implementation, canonical acceptance and a Chinese report explaining both optimization results and actual memory observations. No merge, publication, Rust rewrite, model calls, usage polling, daemon, permanent profiler or persistent schema cache. If the proposed reuse cannot preserve error semantics, repair within scope or retain isolated behavior. Roll back by reverting the scoped optimization/tests and rerunning complete verification and Doctor.
