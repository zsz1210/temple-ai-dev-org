# WI-0233: named permission route qualification

Continue the user's authorization to repair prerequisites before a continuity
comparison. First qualify the installed named-profile route without generation.
This work does not change global configuration, upgrade Codex, enable integrations,
select a model, spend a new experiment budget, or claim a live environment ready.

Use the documented named profile with minimal runtime reads, an exclusive writable
actor root, no command network and no legacy sandbox override. Pass configuration
only to the owned process. Use exclusively synthetic own/sibling marker files;
check direct and symlink sibling reads, sibling writes, own read/write and a real
synthetic Git commit. Compare a reachable owned loopback listener with the sandboxed
connection attempt; only explicit permission denial qualifies a negative control.
Preserve observed failures instead of loosening access or substituting a policy.
No thread/turn/account operation, hidden answer or private file is read by a probe.

Replay malformed responses, invalid configuration, first failure, late generation
signals and cleanup. Keep raw errors and local paths out of retained evidence.
Any passing result qualifies these command controls only: native tool surfaces,
fresh sessions, instruction loading, usage, descendants and launch authority remain
separate. The design's unset model, budget and useful-effect decisions must be
resolved before generation; the latest request is not a fabricated numeric cap.

Acceptance is an honest reusable diagnostic, not a forced runtime pass. Run full
verification and distinct QA. Stop for a genuine unresolved authority boundary;
do not add another model/format matrix. No UI or distributed package change.

Coordination: WI-0190 and WI-0211 have released claims. This item adds only its two
new diagnostic files and own artifacts; their existing test/source files, sealed
experiments and pending PRs are preserved. Sequential work uses no parallel plan.

Basis: [official permissions documentation](https://learn.chatgpt.com/docs/permissions)
and installed CLI 0.153.1 generated schemas. `ThreadStartParams.permissions`,
`TurnStartParams.permissions` and `CommandExecParams.permissionProfile` are declared;
each is mutually exclusive with its legacy sandbox option. Schema presence is not
runtime enforcement. Rollback withdraws only the added instrument, retaining results.
