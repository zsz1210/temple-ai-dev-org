# Contributor entry qualification and Lean closeout

Product candidate: `8518841bdb2b0d66529652507cb9d83516d8b813`.
Base: `1a3b132dc106ca9fb483443fdd2cbdcb0f8e9e7d`.
Branch/worktree: codex/team-entry-guidance, sibling temple-team-entry.
Developer: agent-rikku. Distinct Quality Evaluator: agent-lulu.

## Verification

After the distinct narrow PASS, the coordinator ran one complete frozen-candidate
`/usr/bin/time -p npm run verify` on Node v24.20.0. Exit 0; **1,304 passed,
0 failed, 0 cancelled, 0 skipped, 0 todo**. Suite duration 284,896.648542 ms;
command wall time **286.23 seconds**. Repository checks (118 overlay files,
10 Positions), documentation links and package boundary passed. Package: 455
files, 1,026,273 packed bytes, 3,973,872 unpacked bytes. The only added packaged
file is the team-entry guide; dependencies, allowed roots and byte ceilings stay
unchanged. No Management Console or graphical UI changed.

Raw log: [full-verify.log](full-verify.log), SHA-256
`f043031fb0754a8842e3380a0fc4ee1d17f2925ceb8795be06463833ad564243`.
Retained log equals the original command log by SHA-256. The preexisting recursive
test-runner discovery warning remains visible; the full parent run completed.
Nine changed product files matched committed bytes and executable modes before
the complete run. Candidate revision remained unchanged and worktree clean after it.

[Distinct judgment](verifier.md) retains the actual separate runtime's PASS:
four focused tests and 11 independent scenarios, including same-name ambiguity,
legacy verified team policy, ordinary attribution, multiple blockers and claim
precedence. Baseline JSON objects and exit statuses matched; fixture contents,
modes, modification/change timestamps and directory entries were unchanged.
The coordinator's initial fast checks passed 58 tests; relevant focused rerun
passed four after the missing-task assertion was added. These are separate from
the final full-suite evidence, not substitutes for it.

## Observed behavior and limits

Default text no longer reports complete when actor/task readiness fails. It shows
actor versus task eligibility, selected stable identity, assignment, active claim,
every applicable blocker, responsible actor and next action. A missing/unselected
Work Item is not checked. JSON and execution/authorization decisions are unchanged.
The first-contributor guide links existing setup and recovery procedures without
adding login or bypassing policy. All three README entrypoints are aligned.

[Same-state comparison](readiness-comparison.json) used 12 local CLI calls across
three selected cases. Old/new human text tokens were 1,789/154, 1,275/187 and
2,303/211 (o200k_base). JSON and exit codes matched; selected canonical digests
were unchanged. This is a text-size/readability diagnostic, not provider usage,
billing, overall speed or real independent-human onboarding evidence. Benchmark
calls made no model requests; coordinator and verifier model usage was not measured.
The prior separate-account study had one human operator and is not relabeled.

## Authorized closeout

All bounded design criteria passed; no blocking defect remains in this slice.
No Jev acceptance, model experiment, policy or learning promotion, main merge,
package release, downstream upgrade or human field study was performed.
Canonical WI-0258 records remain in the shared checkout, separate from product
work and other pending Jev records. Product source is committed and isolated.
Rollback is to revert the presentation/docs candidate; preserve the actual evidence.

Next recommendation: integrate the qualified branch, then use the written guide
for a separately identified teammate's small approved task. Record real blockers
and interventions before claiming human onboarding maturity or broader savings.
