# Contributor entry and readiness presentation

User selected Temple multi-person collaboration and onboarding improvement; the
interrupted Jev acceptance proposal is excluded. Existing WI-0250/0252 behavior is
already merged; avoid repeating that development or claiming independent-human
onboarding from the prior one-human/two-account rehearsal.

Reproduced on main 1a3b132d: readiness for terminal WI-0252 returns exit 1 and
task_ready:false, but its human headline says complete and prints the full JSON.
The contributor guide also combines first use with migration, measurement plans,
reconciliation and recovery without a short linked entry for a new teammate.

Deliver a dedicated human readiness rendering in the existing field command
module. Show actor eligibility separately from task readiness, selected stable
identity, task owner/assignment/claim, each blocker code/message/responsible actor
and next action, plus the active-claim assignment note. Without a Work Item say it
is not checked. End with observation-only/no-write limits and --json for full
details. Keep the JSON object, eligibility decisions, exit codes and other field
commands unchanged. Do not execute advice or grant new membership or permission.

Provide a short team-entry guide linked from all three README entrypoints and the
documentation index. Separate coordinator preparation from contributor first use;
explain project-owned identities versus display names, repository access versus
assigned responsibility, no ordinary Temple login, legacy/high-assurance limits,
proposals versus approved work, active claim continuation, full diagnostic handoff,
and reuse of compatible measurements without dropping required acceptance. Link
advanced setup, profile migration and recovery rather than copying them.

Acceptance: command-level cases show blocked actors/tasks accurately, preserve
multiple actionable blockers, distinguish no task from checked task, retain valid
claim precedence and JSON/exit semantics, and perform no canonical writes. Verify
guide inspection commands against disposable existing fixtures. Measure before/
after serialized text size on identical states as a readability diagnostic only.
Distinct Lean verifier challenges boundaries before one frozen-candidate full
npm verification. Package changes add only one guide, with unchanged roots/caps
except one necessary file-count increment. No UI/browser change, authority change,
new model experiment, independent-human study, deployment or release.

Low-risk bounded Lean applies to additive documentation and read-only presentation.
Scope: src/field-commands.mjs, test/field-readiness.test.mjs, team-entry guide,
collaborative-delivery guide, docs index, three READMEs, package allowlist. No route
IDs apply; no affected-path overlap with active Jev artifact tasks. Product work
is isolated in codex/team-entry-guidance; canonical allocation stays in the shared
checkout to avoid reused Work Item IDs. Rollback: revert this presentation/docs
change; existing machine output and state remain available. Stop at qualified
implementation, evidence and organizational acceptance; no merge implied.
