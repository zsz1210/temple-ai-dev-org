# Repository task instructions

- Read `TASK.md` and complete only that bounded product task.
- Change only files below `src/` and `test/`.
- Do not install dependencies, use the network, contact external services, create another task or agent, or retry with another model.
- Run `npm test`, inspect `git diff` and `git status`, and return the requested five-field completion record.
- Product requirements and test results are authoritative; conversation memory is not project state.
