# Three-arm diagnostic comparison

## Authority and outcome

The user explicitly confirmed the proposed step-4 envelope: 12 fresh Terra medium Builder/Verifier stages, at most 960,000 observed Operational Tokens and 72 minutes, with the prior 80,000 Token / six-minute per-stage ceilings. Existing subscription allowance only; no purchases, refill, reset, extra actor-turn retry or fallback. This supersedes no sealed approval. Numerical approval was explicit; final source/protocol hashes are coordinator bindings after generation-free readiness, not hashes the user claims to have inspected.

## Design

Six independent deliveries: ordinary, prior Temple, slim Temple, followed by slim Temple, prior Temple, ordinary. Every delivery has one fresh Builder and one fresh Verifier. One model and effort throughout: gpt-5.6-terra / medium. This reverses pairwise order but does not fully balance position (prior Temple remains central); caches and time drift remain uncontrolled. Two observations per arm cannot establish statistical reliability or enterprise efficacy.

Use the frozen shipping-quote brief, public tests, hidden oracle, completion schema and acceptance procedure from delivery-control-pair. Ordinary uses Git, BRIEF, WORK and handoff; prior Temple uses accepted B/C product sources at 87d68a189d03df33a61df753f441acdb387c7835, before WI-0206/0207. Slim uses accepted WI-0207 product sources. Keep the same runtime/provider/security instrument across all arms. Source-level hooks only expose existing stage execution/assessment; original pair prompts, default policy and historical evidence remain unchanged.

Slim explicitly selects task material and may declare only exact unchanged AGENTS.md/TEMPLE.md bodies actually read and still present in the current actor context. It must read them first; provided hashes alone are not reading evidence. Unknown availability means full acquisition. Do not add a large artificial roster or history to inflate the benefit. Required common benchmark handoff artifacts remain symmetric; report that this can mask workflow simplification benefits. This measures the combined package, not causal attribution to each change.

Retain quality, route adherence, Tokens (input/cached/noncached/output), stage wall time, test/command counts, returned bytes, failures and interventions separately. Never allocate inference Tokens from byte shares or buffered command intervals. Coordinator inference, setup and instrument development are outside the subject allowance and comparison; no extra model judge is authorized. Held-out deterministic checks and fresh Verifier decisions establish observed acceptance.

## Safety and readiness

New runner and regression tests are separate from sealed matrices/labs. Reuse the existing subscription-authenticated Codex app-server, not OpenAI API keys. Validate installed schemas and model/effort availability without model generation. Bind all instrument, policy, source-root, fixture, provider and prompt inputs before launch. Check sandbox/command refusals, immutable fixture/candidate/content/evidence agreement, one-shot approval consumption and aggregate budget accounting before live turns. Distinct Lulu readiness review and full fixed-candidate verify are required. Preserve partial observations and stop the whole matrix on safety/provider/protocol/budget or noncomparable outcome. Do not repair and resume a consumed run.

The only shared edits are additive hooks in scripts/delivery-control-pair.mjs and opt-in material flag validation in scripts/delivery-command-policy.mjs; default pair behavior stays unchanged. New runner/test own scripts/context-material-comparison.mjs and test/context-material-comparison.test.mjs. Records live under WI-0208. No main/PR merge, release, account change or other task's source edit is included.

WI-0172 is an inherited intake-only reservation with no claim; overlap coordination was sent to its owning task before further implementation. An explicit release after Position transition found the claim already automatically released. Initial additive hooks were applied before the subsequent Developer claim; this sequencing error is disclosed, not presented as compliant ownership. Developer ownership was established before all subsequent runner/test implementation.

Readiness attempt v1 had no model calls and failed because the generic 15-second RPC wait was shorter than its 30-second command deadline. A direct installed probe confirmed successful entry in 14.167 seconds. The readiness-only transport wait is corrected to 45 seconds, leaving the command deadline and approved model limits unchanged; a fresh lab/protocol is required. The first candidate nevertheless passed all 640 local tests. Prior unconsumed setup remains retained.

## Step 5 boundary

Prepare scenario designs after results: fresh-session recovery from a handoff; approved requirement change with stale evidence; shared-contract/ownership conflict and integration. Give both arms equal underlying facts and suitable ordinary Git/docs, not an artificially empty baseline. Use explicit freshness, overwrite, duplicate-work and human-question oracles. Local simulated collaboration must not be labeled real multi-human/enterprise validation. No step-5 model budget was approved by the user's confirmation of step 4. Do not silently spend leftover step-4 allowance on these scenarios.

## Decision rule

Publish all arm results, including failures and conflicting repeats. If slim retains acceptance and both repeats use less than prior Temple, describe a directional small-task finding, not significance. A quality regression blocks expansion. If costs do not improve, report the remaining observed operations and keep the feature optional; no post-hoc model swap, extra repetition or efficiency claim. Step 5 tests a different value proposition and must not be presented as rescuing a failed small-task result.
