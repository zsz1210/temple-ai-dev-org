# WI-0192: Measurement repair

The user approved the WI-0191 report's next steps. This bounded local work repairs experiment evidence classification, archives authorization without changing original bytes, and removes inconsistent receipt fixtures. It does not change Temple runtime, route policy, old grades, or the sealed WI-0191 executor. No live run, new budget, merge or release is authorized.

## Acceptance and implementation

- Create a separate successor executor and deterministic tests. Incomplete helper acquisition must not become an unauthorized-write or false-acceptance claim. Preserve explicit safety violations and unknown error causes.
- Retain bounded machine error categories and a digest, not arbitrary provider text, credentials or paths. Unknown causes remain unknown.
- Do not guess test execution from wrapped or combined shell strings. Provide a structured command-array execution receipt from the evaluator's command executor, explicitly separate from subject-run evidence. Both arms get the same evaluation. Historical subject execution remains unknown when not observed.
- Finish fixtures must actually include and run both required test files before recording the candidate. Freeze product/test scope separately from the stale source and failed diagnostics perturbations. Required and optional findings are distinct.
- Preserve the original authorization and approval as archival evidence; the original relative reference is historical, with an explicit archival map and hash verification. English Markdown is a rendering, not a replacement authorization. Do not reuse the spent approval.
- Run focused deterministic tests and full repository verification. Independent acceptance is not claimed by the implementation author.

## Risk and eligibility

Lean, low-risk, bounded experiment-only tooling. The successor run entrypoint stays disabled pending fresh protocol, review and authorization. No new dependencies or external writes. Existing WI-0191 unresolved live coverage remains unresolved. Its frozen source files must match candidate 85beb7acef3c3f9d8fe2678a66f3c3b10c45b2dd; only unsealed report/authorization publication rendering changes.

## Stop

Stop after implementation and local verification/handoff. Do not spend quota to prove native helper availability. A follow-up live proposal must first inspect the exact provider error and freeze the revised prompt/fixture/metric contract; current evidence cannot identify the historical spawn failure's root cause.
