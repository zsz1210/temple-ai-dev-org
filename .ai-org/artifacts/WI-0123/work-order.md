# Work order — WI-0123

Close the exported Execution resolver input domain so every accepted request and option set produces a Route within the declared v1 schema, and malformed direct Route inputs return errors instead of throwing.

The repair covers capability identifiers, resolver metadata options, total collection handling, regression tests, and exact managed-schema digest metadata only. Provider execution, automatic routing, project policy changes, external actions, push, merge, deployment, publication, and release are excluded.
