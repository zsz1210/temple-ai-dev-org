# Risk review — WI-0130

- **Material usage:** six candidate turns plus one evaluator can consume Pro allowance. Generation remains blocked until an exact owner-approved turn and operational-Token envelope exists. Reactive Token interruption is a safety stop, not a billing guarantee.
- **Biased baseline:** condition A retains explicit scope, version control, tests, review evidence, and safety boundaries. It is not intentionally disorganized.
- **Confounded routing:** A and B must use Terra medium; B and C must differ only in pinned versus advisory Execution Profile. The analyzer rejects mismatched evidence.
- **Evaluator leakage:** the evaluator runs from a fresh temporary root and receives no mappings, usage, timestamps, revisions, Temple files, or repository paths before score freeze.
- **Protocol drift:** the runner checks the exact installed non-experimental App Server schemas and performs a no-generation handshake. Drift fails closed with no automatic retry.
- **Filesystem impact:** candidate writes are restricted to exclusive disposable repositories and `src/` plus `test/`. The retained lab is outside the source repository and can be deleted after the evidence is no longer needed.
- **Overclaiming:** two cases qualify only a bounded diagnostic result. Missing, invalid, mixed, neutral, or negative evidence cannot be promoted into an automatic route or framework-wide claim.
- **External effects:** network use inside candidate sandboxes, dependency installation, deployment, publication, release, external writes, purchased Credits, and automatic Credit reload are prohibited.
