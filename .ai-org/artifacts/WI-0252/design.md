# Collaboration integration and test analysis

The maintainer authorized the previous recommendation: combine the qualified
collaboration-recovery and closeout branches, retain both WI-0247 histories, run
integration verification and independent review, prepare one PR, and analyze the
retained rehearsal. Main merge, publication, account changes and a new comparative
model experiment are excluded. Stop at a reviewed PR and the analysis.

## Scope and design

- Base: closeout dd824741dbaa4fceca91976a259db02fc7bb00cb, containing main
  cec7b1f142bf2d928ed902b74c7edaa4fa20caa7 and qualified candidate 9148bd64.
- Port the source, tests, documentation and framework overlay from recovery
  6ca7c69665e59559769f3a402d37dac2f8f190fb relative to its base 6f3d8960.
  Preserve newer changes using a reviewed three-way integration. Do not replace
  main's physical file/mode candidate validation with the older implementation.
- Keep canonical main WI-0247 intact. Archive the recovery branch's colliding
  record, evidence and branch event delta byte-for-byte as explicitly historical
  source snapshots, with Git revision, original path and SHA-256. They are not
  imported lifecycle authority and retain their original IDs and references.
- Preserve narrow descendant allowance, including every intermediate Git edge,
  product scope and dirty-file checks. Only own administration and explicit new
  Markdown evidence qualify; readiness and reports grant no permission.
- Include package checks in this item; combine the two new diagnostic modules and
  ADR-0068 with the existing 448-file manifest, retaining its exclusions and size
  limit. Update framework Skill sources under the existing maintainer workflow;
  do not hand-edit installed managed Skills or the lock. Dependencies and license
  remain unchanged; this is original repository material, not a new capability.

## Acceptance and verification

Contributor proposals, portable diagnostics, safe administrative descendants,
active-claim task guidance and candidate-bound measurement reports must coexist.
The main physical product, hidden-history, stale-receipt and no-write guards must
remain covered. Verify archive hashes and canonical WI-0247 preservation. Run
focused integration tests while editing, then one full `npm run verify` on the
final behavioral candidate; repeat only for relevant correction. Run temporary
init -> doctor -> status for the changed packaging/organization contract and
Doctor after canonical state changes. No Console/UI change is planned.

A distinct Agent on the already-authorized remote Mac reviews the exact merged
candidate and performs bounded probes; prior branch success alone is insufficient.
Record failures and rework rather than erasing them. Use the documented actual
measurements, distinguish command/test/review/wall time, and leave unknown billing
null. The prior two-host toy test has no matched baseline and cannot establish
token savings or production multi-user maturity. Read observed usage counters only;
do not query quota or reset credits.

Rollback is a reviewed revert of the PR's product changes; retain historical
evidence, claims, measurement attempts and current main history.
