# WI-0188 corrected local verification

Behavioral candidate: `6346e02656347feed6b1bfd211562ff3ba6818cf`. This candidate supersedes the rejected first attempt only through the fresh passing evidence below; `verification-attempt-1.md` and `independent-qa.md` remain preserved.

Full `npm run verify` passed repository, documentation and package checks and **630/630 tests**, zero failures/skips, **156.656 seconds**. Log SHA-256: `92ba92af5ce293fa578679594d03325772ea9feca53ddd69ee29d68ad6930767`. Source, tests, scripts, documentation, source templates, installed instructions and lock remained identical to the exact candidate. Doctor: **37 pass, 0 warnings, 0 failures** after rebuilding the plan.

Fresh distinct-Identity QA (`agent-lulu`, Developer `agent-rikku`) records **PASS** in `independent-qa.v2.md`: entry, packet and Skill suites **32/32 passed**, 39.902 seconds, plus installed-launcher counterexamples for draft/unknown/empty/null/absent/object/wrong-case contract states, stabilized controls, structured stale/invalid input and unchanged legacy output. No blocker remains. The first full run's Skill-reference failure and first QA's draft-contract finding are resolved; the allowlist and authority guards were retained.

The real initialized sandbox verifies cold Builder entry, explicit claim/completion, fresh distinct Verifier entry with the exact handoff/evidence, actual product-test invocation and explicit acceptance. It uses installed `templew.mjs` and its supported version-checking `TEMPLE_CLI_PATH` targeting candidate source. Required instruction, policy, procedure and evidence bodies are checked byte-for-byte; entry is no-write and incomplete/ineligible cases retain body-free fallback. This proves installed command routing and acquisition, not model instruction loading, comprehension or autonomous route selection.

Package inventory: **409 files**, **3,466,562 unpacked bytes**. Compared with accepted B's 406 files, only the context-enter module, ADR-0058 and existing core Skill's execution reference were added. No dependency, exclusion or ownership-boundary change. Supported upgrade synchronized the exact managed sources and lock; project-owned root instructions were preserved. Publication audit: zero blocked findings, the same 68 preexisting binary review items; not publication approval.

Both authorized slices are now implemented and verified locally: B composes administrative completion with separate diagnostics; C selects optional material and completion guidance within existing Lean eligibility. B's named administrative harness observed 3 to 1 commands per stage, with nonuniform local timing/byte changes. C introduces no new model measurements. No Token, end-to-end speed or quality superiority is established; model/effort are not applicable to these deterministic tests, and developer/reviewer agents are not benchmark participants.

Stop at C organizational closeout. Live model comparison, merge, publication and deployment remain separate work. After closeout, `npm run verify:fast` passed repository/link/package checks and **54/54 fast tests** (1.070 seconds in the test runner); Doctor remained **37/0/0**. Final changes contain evidence and lifecycle state only.
