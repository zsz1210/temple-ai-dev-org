# WI-0188: bounded Lean entry

The user authorized slice C after successful slice B. WI-0187 is closed with 619 full tests and distinct QA passing on `2611def7db156eab8e1936f5feeefe7c51ade014`; parent integration is `76c0177332bc4a71bce0ea6ebdc10d741844d14c`. This slice connects existing material and completion, without another workflow, automatic downgrade, live model call, merge or publication.

Provide opt-in `context enter` with explicit Work Item, Position, Agent Identity and Principal, mandatory `--no-write --json`, optional existing purpose and stale-preview binding. It selects scoped stage material only for current low-risk bounded Lean Build/Test without UI, unresolved work, conflicting ownership, active runtime workers or unsettled recovery/diagnostics. Reassess profile using current policy, validate eligible actor/sponsor and active claim binding if present, preserve distinct fresh Verifier and exact handoff candidate. Absent claims may route to the existing claim operation; entry never claims work or advances state.

Eligible entry returns the existing provenance-preserving packet plus the installed Lean execution procedure and navigation toward claim or finish. Ineligible, recovery, ambiguous or incomplete acquisition returns explicit reasons and existing compact navigation without projected bodies. Fallback grants no authority. Bind source/authority, relevant runtime state, actor, purpose and resulting route; reject stale expected digest without writing. Preserve ordinary compact Context, full/stage packet defaults and all mutation validators.

Update core source-template entry/temple-work guidance narrowly: callers deliberately opting in may use enter instead of a separate compact preview, consume the returned whole required bodies, and read remaining required references. No bootstrap/provider instruction exemption, proof of reading/comprehension, silent truncation or automatic mutation. Update installed managed files only through supported upgrade, preserving exact ownership and project-owned content. No new Skill or dependency is introduced.

Acceptance: actual installed-template CLI sandbox covers cold Builder and fresh Verifier with no prior packet, claim and finish through explicit commands, correct candidate and separate Identity; negatives cover Standard/high risk, ambiguous contract, wrong actor/Principal, claim conflict, stronger profile, overlap, active workers, pending recovery/diagnostics, missing or unsafe sources and changed bindings. Verify whole mandatory instructions/procedure/evidence coverage and no canonical writes from entry. Native provider entry file installation is verifiable without model inference; model instruction loading and autonomous model routing remain unproven. Run full repository verification and distinct QA. Stop after C local closeout; a new comparison is separate work.

Rollback reverts optional entry/guide/template changes and refreshes managed sources through the supported upgrade route; retain prior evidence. Expected package growth: context-enter module, ADR-0058 and one core Skill reference, subject to actual inventory review.
