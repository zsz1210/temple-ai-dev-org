# WI-0187: composed completion

The user explicitly authorized slice B and continuation to slice C after B passes. This Work Item implements and verifies B only, using WI-0183's proposal and accepted WI-0186 acquisition as context. No live model comparison, merge, publication, provider change or external action is authorized here.

Add an opt-in stage-completion CLI for bounded local low-risk Lean Build/Test work without UI or active runtime workers. Developer completion reuses the existing validated delivery. Verifier completion requires its own active eligible sponsored claim, a different Identity from the exact Developer handoff, the exact candidate, explicit acceptance judgment and existing test/closeout evidence; shared release/transition preparation supplies the canonical writes. This is Lean acceptance, not formal Independent QA.

Compose full Status and Doctor after mutation, with separate mutation and diagnostic outcomes. A diagnostic exception/non-pass cannot erase committed lifecycle facts or produce overall success. Persist scoped pending/failed diagnostic attention including terminal work; replay must validate the same operation, resulting state, evidence and authority before repairing diagnostics only. Reject drift, changed requests and ineligible actors. Do not duplicate transitions/handoffs after interruption. No arbitrary test-command execution, silent profile downgrade or automatic judgment.

Validation: actual CLI normal/rejection paths; equivalence to individual operations; every journal persistence and diagnostic failure boundary; changed candidate, evidence, authority, identity, claim and scope; no-op replay and visible terminal diagnostic failure. Run complete repository verification and separate Identity QA. Measure deterministic administrative command counts only; do not claim Token or model speed savings. Preserve old deliver behavior and previous sealed evidence. Stop B after local organizational closeout, then create dependent C under the user's existing authorization.

Rollback: revert the new optional entry and related helpers/tests/docs while preserving canonical records and prior evidence. No mandatory instruction or Skill changes in B; C owns entry integration. Expected package additions are one source module, one ADR and one extension guide; update the inventory ceiling only after actual dry-run comparison.
