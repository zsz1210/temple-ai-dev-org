# Approved scope — WI-0122

The approved slice is limited to:

1. clarifying pinned-versus-capability unresolved-reason precedence;
2. aligning resolver output, both managed schema copies, and semantic validation;
3. closing the two additional low-severity provenance/string gaps;
4. expanding focused regressions; and
5. refreshing only the exact managed schema digest metadata.

The original WI-0121 QA failure stays immutable evidence. Provider execution, automatic model selection, project-owned policy changes, UI, external integrations, push, merge, deployment, publication, and release are excluded.
