# Work order — WI-0030

## Purpose

Correct the WI-0029 Quality NO-GO without weakening the command gateway's transient confirmation or exact provider-delivery behavior.

## Design

- Keep the complete normalized instruction only in the transient request path needed for local confirmation and the single provider call.
- Replace retained instruction content with a metadata-only summary such as an omitted-content label and character count. Do not retain prefixes, suffixes, reversible hashes, or any value that reproduces a complete short instruction.
- Rename the Dashboard history presentation from `Retained preview` to language that truthfully describes a non-content retained summary.
- Preserve bounded input, secret-pattern rejection/redaction defenses, idempotency, expected-state checks, provider acknowledgement, delivery-unknown handling, and the no-automatic-retry boundary.

## Risk review

- Very short inputs are the adversarial boundary: prefix truncation and ordinary hashing can still disclose or make them cheap to recover.
- The provider still requires the exact instruction after explicit confirmation, so the fix must not alter the in-memory provider payload.
- Durable fixtures, logs, events, errors, and Dashboard history must not introduce a second copy of the raw instruction.
- Real Codex execution, remote control, new task creation, push, release, and publication remain outside this corrective Work Item.

## Verification

- Test one-character, ordinary short, boundary-length, long, and secret-bearing instructions.
- Assert the provider receives the exact accepted instruction once while durable command state and history cannot reproduce it.
- Run focused Control Plane tests, full repository verification, schema validation, Doctor, and a fresh local Dashboard runtime review on the exact integrated candidate.
- Quality verification must be performed by Lulu, independently from Rikku's implementation.
