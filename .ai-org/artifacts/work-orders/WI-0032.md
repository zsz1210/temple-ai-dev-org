# Work order — WI-0032

## Purpose

Restore the claim that federation participant inspection is local, read-only, and free of participant-triggered execution or network activity.

## Technical design

- Resolve and compare the participant path with the exact Git worktree top level before any canonical read.
- Invoke Git with a bounded allowlisted environment and noninteractive behavior; do not forward unrelated ambient credentials or Git injection variables.
- Disable executable repository behavior needed by inspected commands, including external `core.fsmonitor`, and disable lazy fetch.
- Preserve literal-object revision reads, symlink refusal, size bounds, unknown-safe failures, and participant immutability.

## Verification

Use hostile disposable repositories covering external `core.fsmonitor`, a nested participant path, injected Git environment, missing promisor objects, and dirty canonical root paths. Assert fail-closed output, no marker execution, no network request, and no participant mutation. Run focused, full, schema, Doctor, and fresh Independent QA.
