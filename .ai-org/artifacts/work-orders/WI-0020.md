# Work order — WI-0020

## Purpose

Correct the WI-0016 Independent QA no-go without weakening the audit-export exclusion contract.

## Design and risk review

- Resolve and verify the canonical event path against the real repository root; reject a linked file or any linked parent component.
- Treat allow-listed scalar fields as typed data. Preserve only bounded values that match the field's documented identifier, enum, boolean, numeric, timestamp, revision, or safe-reference grammar; redact arbitrary token-shaped payload text.
- Add the exact hyphenated secret-marker and parent-directory-symlink reproductions as regression tests.
- Record exact before, upgraded, rolled-back, and recovered digests in the disposable Phase 4 rehearsal.
- Do not export prompts, responses, hidden reasoning, credentials, provider payload bodies, tool payloads, or recovery before-images.

## Verification

Focused audit and recovery tests, full repository verification, fresh cross-assigned Independent QA, and exact digest evidence.
