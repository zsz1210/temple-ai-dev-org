# Work order — WI-0026

## Accepted scope

Ship the Phase 4 project contract and Alpha.27 metadata. Add an empty project-owned federation registry, Draft 2020-12 registry/portfolio schemas, upgrade-time creation without adoption or overwrite, capability flags, version constants/package metadata, CHANGELOG entry, and focused installation tests.

## Technical design

- Export `emptyFederationRegistry()` and `ensureFederationRegistry(target)` from `src/federation.mjs`; use exclusive atomic create and preserve an existing registry byte for byte.
- Seed `.ai-org/project/federation.json` in the distributable overlay as project-owned state. Fresh init receives it through the existing overlay walk; upgrade explicitly ensures it for older projects.
- Add `federation.schema.json` and `federated-portfolio.schema.json`, catalog the registry as required project-owned and the portfolio as optional generated state.
- Add capability flags: `backup_retention`, `redacted_audit_export`, `usage_qualification`, `provider_attach_outcomes`, `repository_federation`, and `read_only_portfolio`.
- Bump the release contract to `0.1.0-alpha.27` in constants, package metadata, and CHANGELOG. The Integration Owner will reconcile all repository-wide version assertions, roadmap wording, and the self-host `temple.lock` after joining both worker commits.

## Verification

Focused tests must prove fresh init seeding, older upgrade seeding, byte-for-byte preservation of an existing registry, schema acceptance/rejection, capability flags, and version metadata. Run full verification and diff checks; report expected failures caused only by Integration Owner-owned repository-wide version assertions if any.

## Risk review and rollback

The project-owned registry must never enter `managed_files` or be overwritten. Schema ownership must match runtime authority. Revert the contract commit; do not delete an existing project-owned registry during rollback.
