# Work order — WI-0022

## Purpose

Correct the WI-0018 Independent QA no-go so a participant can never project working-tree or symlink-target content as an expected Git revision.

## Design and risk review

- Reject symlinks in canonical file and directory paths.
- Read participant canonical documents from the expected Git object, or otherwise prove their bytes are exactly bound to that revision before projection.
- Dirty, missing, unreadable, invalid, or revision-mismatched sources degrade to explicit unknown diagnostics.
- Preserve the read-only boundary and add the exact internal-symlink reproduction as a regression test.

## Verification

Focused federation tests, full repository verification, isolated repository hash comparison, and fresh cross-assigned Independent QA at the exact candidate revision.
