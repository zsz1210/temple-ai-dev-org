# Shared fixture critical-path performance work order

The maintainer approved the next recommendation after WI-0238: measure shared initialization on the complete suite's critical path and optimize the actual bottleneck. Baseline is `91b45470`, whose unchanged behavioral candidate `472c167396c676c7feb0695729f03707be581854` passed 1238 tests. Its historical 342562.26925 ms observation is context, not a controlled before measurement.

Scope is test-only: `test/helpers/lean-delivery-fixture.mjs`, explicit opt-in consumers selected from measured slow files, new meaningful fixture controls and work evidence. Preserve fresh default initialization, all existing scenario bodies, actual tested CLI/finish/verification calls, workflow profiles, actor separation, immutable candidate evidence, full discovery, timeouts and cleanup. No production, package, policy, live experiment or dependency changes. No model calls, account polling, merge or publication.

Measure a new full baseline and final candidate with the same Node v24.20.0 runner, default test concurrency and a read-only extra reporter recording per-file durations. Run the final candidate comparison inside the required `npm run verify` by forwarding the same reporter arguments to `test:full`; confirmed argument forwarding avoids a redundant full run. Repository/package checks remain included. Avoid concurrent benchmarks. An independent reviewer may read source while full verification runs; timing evidence must disclose any concurrent work. One baseline full measurement and one final complete verification/comparison are planned, plus focused editing checks and necessary failed-candidate corrections. A single pair is directional evidence, not a universal speed guarantee.

Measured baseline: 1238 tests passed in 626213.369708 ms. The opted-in files are context-enter (415532.5 ms), lean-finish (297369.5 ms), mechanical-completion (186584.4 ms) and context-packet (177642.0 ms). These share the same compatible setup and test later behavior. Other slow files use different setup or have not been qualified; this wave does not expand into them. File durations overlap and must not be summed as wall time. Actual baseline raw output and per-file summaries will be preserved with final evidence.

Candidate design: opt-in per-test-process reuse of an immutable initial repository at Build before any claim. Each scenario copies product, organization data, configuration and Git state, then executes a new real CLI claim. No active claim, completed product verdict, mutation outcome or runtime is reused. The unchanged synthetic parser's initial developer observation may be copied with its identical candidate bytes as fixture data; it is not new test execution. Mark copied setup provenance explicitly. Preserve the existing fresh `fixture()` default for initialization-sensitive or unqualified callers.

Use exact workflow-profile and ordered affected-path options as cache keys, coordinate concurrent first preparation, never expose mutable cached objects, clean failed setup/copy/claim and remove templates after the test process. Test sibling/template isolation, independent claims, different profile/path keys, concurrent users and cleanup on injected copy failure. Opt-in only consumers whose subject is later lifecycle/context behavior and whose environment assumptions remain compatible.

Acceptance requires original scenarios retained; actual full before/after timings and subprocess/setup observations; passing isolation/negative controls; final complete verification; distinct Agent Identity review; Doctor and evidence-only fast checks. Developer agent-rikku, Independent QA agent-lulu, Principal human. Finish the bounded local candidate and report actual aggregate effect, including a negative or inconclusive result if warranted. No further optimization wave follows automatically.
