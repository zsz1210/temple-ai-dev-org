# WI-0239 — Matched instruction comparison execution

## Authorization and immutable boundary

The maintainer's current “GO” approves the six-subject proposal linked in the
preceding completion message. This is fresh execution authority for the accepted
WI-0238 candidate, not reuse of a retired approval or permission inferred from
organizational closeout. The approved proposal and its full controls remain at
[WI-0238 comparison](../WI-0238/comparison-proposal.md).

Execute exactly the existing unconsumed protocol
`sha256:dafc7fa80cdb274770c8bb339df2bb74a5067f615ce783c02856e80b5804cd27`.
The private lab is identified by its existing suffix `temple-continuity-live-Q2n9Ja`;
its absolute local coordinates stay out of the published evidence. No preparation,
protocol, instrument, fixture, instruction, oracle or library change is authorized.
Current executable/instruction candidate: `af5a984aee6706ab308c693d95e138134162d5d6`;
evidence-only parent: `a69a5080978b28ef482753ba70dea8d0b6960748` (PR #80, unmerged).

## Execution and stop

- Six Terra medium Developer subjects: stable ordinary/previous/current, then
  changed-spec current/previous/ordinary. The first subject is the planned native
  canary, not an extra sample. Use the existing approved runner exactly once.
- Per subject: 100,000 Operational Tokens / eight minutes. Batch: 600,000 / sixty
  minutes. These are inherited stop ceilings, not projected usage or prices.
- Pro subscription authentication only; no Credits purchase/refill, reset,
  replacement, retry, fallback, model/effort substitution or extra judge call.
- Preserve valid product failures as results. Shared validity failure, missing
  usage, source drift, uncertain cleanup or limit exhaustion stops the batch.
  Do not repair or restart it inside this approval.
- Preserve every attempted and unattempted subject, reason, accounting status and
  seal. Stop after this one comparison and independently reviewed report. No merge,
  release, further experiment or default routing change is authorized.

## Acceptance and report

Compare each requirement condition across ordinary, previous and compact Temple.
Report exact product/regression/protected-source results, administration/handoff,
input/cache/output/Operational Tokens, complete-attempt time, command categories,
available acquisition evidence and intervention separately. Unknown is not zero.
Do not rescore or pool older experiments. One sample per condition/treatment,
fixed order and uncontrolled cache only support bounded diagnosis.

Use the project-documentation Skill for an English, evidence-backed report aimed
at the maintainer deciding the next improvement, not a marketing claim. Keep raw
provider data local; retain body-free hashes, numeric observations and conclusions.
Distinct QA (Lulu) reviews provenance/arithmetic/limits without more model subjects.
The same reviewed runtime already passed 767 tests in WI-0238; reuse that exact
evidence, run read-only fingerprint preflight and evidence-only fast checks/Doctor.

## Preflight and ownership

Fresh preflight on 2026-09-07 UTC: same protocol/instrument digests, both 744-file
runtime hashes, only four instruction differences, six-subject matrix and absent
consumed/run/seal files passed. Prepared/current Codex both `0.153.4`.
Account reports Pro, 55% of its available weekly window used, no reached limit,
Credits balance zero. No conversion from remaining percentage to Token capacity
is available; the runner still stops on a provider limit and never buys capacity.

Only WI-0239 evidence/state and the approved private subject roots may change.
WI-0211 has a released claim; bilateral coordination is recorded. Rikku performs
the bounded execution, Lulu evaluates/reviews, and Mog owns integration. Existing
results, source, PR #80 and unrelated worktrees/processes remain untouched.
