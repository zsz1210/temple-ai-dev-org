# Experiment verification and evaluation

Instrument source remains the verified candidate
`3bfae21d8b2cc39df325f025b0e02f4a2ab61669`; execution used `7b73c8fc`.
No behavioral source changed after the 794-test source verification or during the
single approved invocation. Readiness and distinct review are recorded in WI-0245.

The runner returned exit 1 after one completed subject and sealed the original
`delivery-source-drift` rejection/shared validity stop. Final usage was observed,
server exit confirmed and background terminals empty. The changed-spec subject
remains not-run. Hashes of protocol and run match the seal before and after
read-only diagnosis; no old or current result was rewritten.

Git inspection confirms the sole unallowed candidate-to-delivery path is the
evidence registry; product/test bytes did not change. Two new normalized records
are present (Git and Test); the finish request references their local artifact
rather than normalized IDs. Per-operation cost and model intent remain unknown.

A fresh no-hardlinks disposable clone at the exact product candidate was passed
to the existing assessor with default product-only validation and the isolated
oracle executor. It passed 46 cases and regression checks. This separate offline
diagnosis does not waive the original delivery-scope rejection. The clone/oracle
scratch was removed; no provider model turn was sent.

[Report](report.md) and [numeric export](comparison.json) distinguish original
acceptance, post-hoc product scope, missing changed-spec coverage and historical
arithmetic. Evaluation: **inconclusive two-condition study; no demonstrated
efficiency improvement**. Closeout should retain this outcome, not claim a
successful two-subject trial or grant another experiment.

An independent review of the final report and numeric export is required before
closeout. Source/full-suite verification is reused, not repeated for evidence.
