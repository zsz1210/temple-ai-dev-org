# Approved Solo policy and retained recovery boundary

The human explicitly approved Solo with ordinary `attributed` responsibility and
continuing distinct verification. The pinned CLI applied the refreshed preview
fingerprint `sha256:fc9811fca1c539a486dd082106b7bfdb3ae4f47a1686dba094d69cd52c209364`.
Only the project-owned collaboration policy and its event changed. The current
Solo effective ordinary mode remains attributed; High-Assurance and explicit
strict operations remain verified. A future team transition must review this
now-explicit policy. No framework defaults or provider authentication changed.

An identical Developer finish retry was rejected because HEAD advanced from the
product candidate to the administrative evidence commit. Read-only journal
comparison also found three changed inputs: the verification note (amended after
the first finish), the event log, and the human-approved collaboration policy.
The parent should not have amended bound evidence or committed administrative
records while diagnostics remained pending. This is not a README test failure.

The recovery contract requires matching candidate, inputs and resulting state.
Existing record reconciliation does not reconcile local Lean finish journals.
No journal, receipt, claim, gate or canonical Work Item was manually changed to
force success. No next-stage claim or distinct verification has occurred.

The original actor-policy warning is gone. Doctor still reports the retained
failed finish and a stale generated parallel plan. Rebuilding a plan cannot fix
the exact-revision/input mismatch. The draft PR must remain unmerged until an
authorized recovery design resolves changed inputs without discarding history,
weakening current authority checks or presenting old evidence as new acceptance.

Recommended next scope: add and independently test an explicit, fingerprinted
recovery path for an applied lifecycle with changed diagnostic inputs; require
unchanged product bytes or rework, current actor validation, and preserved original
receipt plus a new reconciliation record. Do not broadly ignore warnings or relax
all candidate guards to unblock this one task.
