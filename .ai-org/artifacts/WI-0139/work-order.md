# WI-0139 work order

## Outcome

Repair the Context Capsule comparison contract before another Provider-backed run. The successor experiment must score canonical facts rather than prose formatting and must describe reasoning-effort evidence truthfully.

## Authorized slice

- define typed recovery facts for the single-repository and coordinator multi-repository scenarios;
- evolve the current harness and focused tests while leaving every retained WI-0138 artifact unchanged;
- replay retained WI-0138 completions through a generation-free compatibility projection to demonstrate the former false negatives;
- validate that wrong identifiers, counts, revisions, actions, or authority still fail;
- validate the installed Codex App Server request schema and acknowledge configured model and effort before generation;
- freeze a successor live protocol only after all generation-free checks pass.

## Evidence boundary

The official OpenAI model guidance requires reasoning effort to be selected intentionally and evaluated on representative work. The installed Codex App Server schema accepts a turn-level `effort` override, while its thread `reasoningEffort` field explicitly describes configured or persisted state and is not per-turn execution telemetry. The experiment therefore records three separate facts: requested effort, acknowledged configured effort, and effective execution effort when available. It must not infer the third from the first two.

## Stop condition

Stop after the evaluator, harness, documentation, and generation-free readiness evidence pass independent verification. Do not start a Provider generation, reuse the WI-0138 approval, publish, release, merge, or reinterpret the historical WI-0138 result.

