# Product specification — WI-0117

## Question

On the same two bounded engineering tasks and the same GPT-5.6 Luna Medium profile, does Temple's lean routed workflow preserve objective quality, and what descriptive difference appears in operational Tokens, elapsed time, human intervention, and rework versus a minimal responsible workflow?

## Fixed sample

- Cases: `idempotent-command` and `compatible-event-evolution`
- Conditions: `temple-lean` and `minimal-responsible`
- Candidate attempts: four, sequential, one per case-condition cell
- Evaluation: one separate fresh-context arm-neutral Luna Medium turn
- Retry and fallback: zero
- Comparison rule: a pair enters resource comparison only when both candidates pass objective hidden acceptance and the independent evaluator protocol qualifies

## Acceptance

The exact fixture, framework, protocol, App Server schema, model, effort, task order, instructions, limits, and digests are frozen before generation. Every candidate yields exact Git state, tests, usage, latency, and an arm-neutral package or one terminal fail-closed result. Evaluator scores are bound to a hashed manifest and frozen before condition mapping is unsealed.
