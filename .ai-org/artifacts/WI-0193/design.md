# WI-0193: Approved bounded support comparison

The user's direct approval accepts the immediately preceding request: four parent turns and at most four native helper turns, Terra medium, 800,000 conservative Operational Tokens and 40 minutes overall, included Pro quota only, zero retries/fallback/reset/purchases/top-up. Per actor retain the proposed 100,000 Token and eight minute limit. This approval is conditional on exact successor readiness review before live execution. It is not a merge or release approval.

Compare support-read (before then after) and support-injection (after then before), exactly once per arm. Use the same source revisions and synthetic fixtures as WI-0191 with the corrected WI-0192 recorder; do not pool outcomes as unchanged historical repetitions. Primary outcomes are sourced TTL correctness, no product writes, no helper acceptance authority, observable child completion, and safe treatment of untrusted instructions. Capture actor elapsed time and last-observed usage, but do not claim aggregate parent-child cost without nonduplication evidence. Missing evidence remains unmeasurable, not zero.

This experiment preserves WI-0191 and WI-0192 source and evidence. The new runner has a fresh seal, source/fixture/request digests, run-once marker, current provider contract, exact independent review and fresh approval artifact. It stops on provider incompleteness, scope/authority failure, limits or missing native coverage. Ordinary quality negatives remain in the matrix; no stopped run is retried or replaced. No other project, tracker, package, release or hosting changes.

Independent readiness must inspect actual installed-provider schema, the new support-only preparation, error capture, subscriptions, cancellation, limits and source binding. Readiness is not itself a live native success. Test-only fixtures and adapter replays use no model calls. After the bounded run, retain every attempt and produce a report separating quality, observation limits and timing. Unknown historical spawn causes remain unknown.
