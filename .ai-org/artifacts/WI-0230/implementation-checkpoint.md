# Field remediation implementation checkpoint

This is Developer evidence before full integration verification, not independent acceptance.

## Implemented surfaces

- F01-F05/F12-F13: actual member/claim resolution, handoff Principal provenance, actual Developer independence, workflow risk default, attributed/verified policy, readiness, idempotent contributor setup and fingerprinted profile adoption.
- F06-F07: versioned complete-input measurements, immutable attempts, conservative reuse, trusted-local argv checks, retained confined-node restrictions and CLI/CI completion status.
- F08-F10: truthful attention and structured missing conditions, historical exact-byte evidence archives, schema-validated three-way reconciliation and interruption recovery.
- F11: lifecycle/common-entry callers, CLI, operating contract, routed Work Skill, public guides and three README entrypoints aligned.

## Focused evidence

Node v24.20.0 on macOS. These are separate editing checks, not a full-suite total:

| Check | Observed result |
| --- | --- |
| Actor core plus model/governance/onboarding compatibility | 31 passed |
| Measurement core plus report compatibility | 26 passed |
| Evidence/reconciliation/attention core | 22 passed |
| Root CLI/lifecycle/Lean-delivery integration | 21 passed |
| High-Assurance/autonomous/lifecycle integration | 15 passed |
| Browser gate | Four viewports, six primary views, reduced motion and six synthetic delivery-attention states passed |
| Package boundary | 443 files; 996400 packed bytes; 3869499 unpacked bytes in the observed check |

The synthetic first-use case performed a real local product edit and test: first claim 162 ms, first edit 162 ms, first verifiable output 198 ms, lifecycle segment 1880 ms. Extra framework login: 0; manual human interventions: 0. This is one deterministic fixture, not a real-person onboarding or model-speed benchmark. Token usage was not observed.

Negative evidence includes malformed/unsupported CLI options before execution, wrong claimant, actual Developer's own human approval, expired membership, changed measurement inputs and failed attempts, archive tampering/path escape, conflicting record IDs, stale previews, and actual process termination during reconciliation. Source-specific tests carry the complete case definitions.

## Remaining integration work

Run full `npm run verify` on the committed behavioral candidate, final browser gate, installation/upgrade rehearsal, canonical Doctor and distinct Independent QA. Current parallel child claims share the integration branch while modules are being joined; release them through the supported lifecycle before final Doctor. Do not call this temporary reservation state healthy.

Windows command execution remains explicitly unavailable pending its process cleanup adapter and real Windows validation. Independently operated human machines, unavailable historical source commits, and live provider authentication are not proven by the local fixtures. Trusted-local execution is not a sandbox. No downstream project, hosting permissions, grants/expiry, package publication or deployment was changed.
