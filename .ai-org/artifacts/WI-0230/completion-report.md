# Field remediation completion report

Implementation, independent acceptance and canonical closeout are complete for the frozen 13 findings, three implementation batches and 16 local acceptance scenarios. WI-0230, WI-0231, WI-0232 and WI-0233 are `done`, with exact candidate evidence and rollback records. The exact behavioral candidate is `8bfe38892283744ba0c71bec0cd25f7cf1a02986` on `codex/field-remediation`.

The delivered behavior includes contributor attribution and stable Agent IDs, profile migration previews, actionable readiness and recovery diagnostics, per-task risk defaults, immutable reusable measurements, explicit command adapter capabilities, durable historical evidence, invariant-checked collaborative reconciliation and truthful delivery attention. The operating contract, Skill, CLI, Console and executable public examples are aligned. `docs/planning/field-remediation.md` and ADR-0067 define the frozen scope; `verification-report.md` maps all findings to their acceptance evidence.

Final `npm run verify`: 1250 tests passed, zero failures/cancellations/skips/todos, 285611.118583 ms on macOS / Node v24.20.0. A distinct independent reviewer accepted all sixteen supported local cases and the unchanged named WI-0231/WI-0232/WI-0233 module scopes in `rework-02-independent-review.md`. Three defects found by independent review were corrected before final acceptance. Earlier failures and their raw compressed logs remain in the artifact directory, indexed by `verification-metrics.json`.

After all four closeouts, the parallel plan and status were rebuilt. Doctor reported healthy: 37 pass, one warning, zero failures. The sole warning explicitly preserves this existing project's absent actor policy and legacy verification requirements until an authorized policy transition. It is not a failed implementation check or silently relaxed configuration. No active field claims, active field workers or unresolved field items remain. `release-record.md` records organizational acceptance, not external release. Post-candidate changes are limited to administrative records and evidence/report prose.

Compatible browser measurements cover four layouts, six primary views, reduced motion and six synthetic attention states. The independent reviewer inspected the real screenshot and source applicability. Fresh initialization and legacy upgrade rehearsals passed, preserving old policy bytes and a project-owned file without network calls. Those earlier measurements are explicitly reused for unchanged scope, not reported as rerun on the final candidate.

Observed first-use automation timing was 393 ms to claim, 394 ms to actual product edit, 460 ms to verifiable output and 3948 ms for the synthetic contributor lifecycle, with zero extra framework logins and zero human interventions. These are synthetic local observations, not human onboarding or model productivity claims. Token use and monetary cost were not measured; no account usage was polled.

Windows command execution remains explicitly unsupported. Same-machine clones and synthetic contributors do not establish independently operated multi-machine adoption; external authentication and unavailable-device cases remain labelled fixtures. Existing project policy and external hosting permissions were preserved. No downstream configuration, publication, deployment or main merge is part of this completion.

Recommended next slice: prepare the next Alpha candidate, then run one real new-contributor task from a clean clone and measure time to first output, clarification count, blocker causes and repeated checks. That recommendation is not another task authorization.
