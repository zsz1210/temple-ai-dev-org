# WI-0194: Complete local event-recorder repair

The user requested completion after the WI-0193 recorder failure. Scope is a separate local successor recorder and generation-free validation; no frozen experiment is edited, no spent approval reused and no live comparison started.

Classify the entire installed ThreadItem union explicitly. Content-only items may be observed; native command/file/collaboration retain their guards; declared external capabilities are rejected with named reasons; unknown schema items require review rather than silent acceptance. Subagent activity is a hint, not proof of authorized spawn, model, turn completion or usage. Buffer early child events only within the existing bound and bind only through validated parent spawn completion. Preserve unconfirmed cleanup for unbound actors.

Record bounded safe metadata before schema validation and handlers: allowlisted method/type labels, hashes of opaque IDs, status and fixed error codes. Never persist arbitrary error text, reasoning, prompts, tool arguments or unknown labels. Preserve first failure separately from a bounded recent ring. Audit ordering includes early events, invalid schemas and malformed/unknown items.

Acceptance: test every installed item kind, schema drift, parent/child ordering permutations, interrupted/failed spawn, malicious authority attempts, missing child terminal, privacy and retention caps. Replay complete notifications through the real runSubject orchestration with a local fake transport and actual installed notification schemas; a permissive empty schema alone is not sufficient evidence. Use deterministic checks rather than another model run. Verify old bindings unchanged, full repository regressions and distinct review. No production behavior, dependency or Skill changes.

Lean low-risk bounded artifact-only work. Ordinary handoff/transition applies; composed Lean deliver excludes artifact-only scope. Stop after implementation, local verification and review; live compatibility and missing comparisons remain explicitly unproven. A later live probe needs its own reviewed protocol and applicable budget authorization.
