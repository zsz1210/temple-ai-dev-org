# WI-0227: smaller whole operating contract

The user authorized implementation of the [WI-0225 follow-up](../WI-0225/composition-audit.md)
with "go". This Work Item implements that bounded contract refactor, not the
withdrawn WI-0226 operation-material selector.

## Scope and acceptance

Edit only the distributed `TEMPLE.md`, its `AGENTS.md` routing, and a dedicated
local test. Existing installed/native project-owned instructions remain untouched.
No CLI modes, workflow gates, Skill bodies, models, integrations or default policy
values change. All instruction sources continue to be acquired whole.

Keep authority, exact ownership, scope, candidate-bound evidence, distinct Verifier,
bootstrap, recovery, escalation and stop rules. Replace duplicate command recipes
and background catalogs with references to existing installed procedures and policy.
No new instruction file or package dependency is introduced. Map old obligations
to the retained contract/native instructions/procedures before adoption.

Measure the same fixture before/after for both Builder and distinct Verifier,
including all required instruction reads and any new reference overhead. The normal
cold path must shrink in complete serialized output, not only raw source bodies.
Initialization/recovery/exception destinations must remain available. Test normal
installation, old whole-source handling and project-owned upgrade preservation.
Run the full local suite and obtain a separate semantic review before acceptance.

## Risk and stop boundary

This is a Standard instruction-maintenance candidate, not permission to weaken
behavior. A missing obligation or a larger end-to-end cold path rejects it. Unknown
and independently required sources retain whole-source acquisition. No model
experiment, retry, account action, release, merge or publication is included.
The baseline instruction files and existing upgrade conflict protection are the
rollback path; never repair ownership by changing a real project's lock manually.

Source overlap coordination: WI-0190/WI-0211 claims are released; WI-0226's retained
reliability files/evidence remain unchanged. Only this Work Item's named paths are
edited. Historical designs and measurements are not rewritten.
