# Implementation and local verification

Behavioral candidate: `df40202bcc44a97494e67acb2cc743b03ec3a012`.
Developer: agent-rikku; independent review belongs to agent-lulu.

- Expanded focused group: 42/42 pass, 2277.956875 ms.
- Full `npm run verify`: 687/687 pass, no failures/skips/cancellations,
  165733.613459 ms.
- Maintenance seed preserves public behavior but fails named undefined/null
  option checks; minimal reference repair passes the unchanged hidden oracle.
- New runner binds eight subjects/sixteen stages, per-stage diagnostic keys,
  source/fixture/request/design hashes, original command gates, and exact eight
  scratch-directory exclusions. Old runners and sealed laboratories are unchanged.

One initial check rejected a non-English quote in the authorization document;
its English rendering was committed before this successful full verification.
The first preparation is obsolete due that source revision update. Neither it
nor the current laboratory has consumed a live approval at report creation.

Fresh installed config isolation checks passed with zero model turns. The current
generation-free probe initially returned command-nonzero while full verification
was active. A standalone first-subject context command with the actual probe
environment subsequently passed. Root cause is unproven; retain the failure.
Independent live readiness still requires the complete eight-subject sandbox
artifact. These checks are not live experimental results.
