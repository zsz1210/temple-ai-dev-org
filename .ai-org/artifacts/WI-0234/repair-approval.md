# Native execution repair and fresh comparison approval

The maintainer explicitly approved repairing the withdrawn instrument and continuing
the experiment. This resolves the pause for implementation, not runtime qualification.
Retain the failed v1 protocol and all seven attempts unchanged. They supply no valid
comparison samples. A new exclusive v2 lab/protocol must be frozen before generation.

The renewed run keeps the existing envelope: eight Terra medium subjects maximum,
800000 Operational Tokens and sixty minutes; 100000 Tokens/eight minutes per subject.
Use Pro capacity only. No purchase, refill, reset, fallback, replacement or retry.
The first planned subject is the native-execution canary, not an additional probe.
Do not dispatch subject two unless the canary has observed completed native commands,
complete usage, confirmed cleanup and a current inspectable candidate. Correctness
failures with a functioning instrument remain results, not reasons for replacement.

Restore only the required native code-mode host; retain external-tool, shell,
filesystem and network isolation. Coordinator command/exec controls are not native
model-tool qualification. Permit null candidate/test fields for an honestly blocked
handoff; stop the batch on absent native execution or blocked candidate production.
An AbortSignal must interrupt the owned turn, preserve usage and prevent next dispatch.
Run regression checks and independent source review before the fresh attempt.

WI-0190 and WI-0211 remain at Release Gate with released claims. This repair only
changes the previously scoped runner, its tests and WI-0234 artifacts. No publication
or broader framework/model-policy change is authorized. Developer remains Rikku.

Official contract: [Codex App Server](https://learn.chatgpt.com/docs/app-server)
distinguishes command/exec from a model turn, reports commandExecution completions,
and provides turn/interrupt plus background-terminal cleanup. Installed Codex 0.153.1
lists code_mode_host as stable/default-enabled. Actual native operation still needs
the canary; configuration and offline tests cannot prove it.
