# WI-0235 — Bounded delivery refinement

## Authority and outcome

The user approved completing the improvement order after WI-0234. Implement the existing small-task execution recipe, separate blockers from downstream work, and add privacy-bounded overhead observations. Stop after offline qualification and the Developer handoff. No new model generation, routing policy, profile downgrade, publication or release is authorized by this record.

## Scope and acceptance

- Use one reading route and one completion operation for eligible Lean work. Preserve native/bootstrap instructions, required authority reads, claims, real tests, exact revisions and distinct verification.
- Keep the existing canonical handoff fields. Clarify actual unresolved issues versus next-owner work; never reinterpret arbitrary prose automatically.
- Version the future experiment completion contract: explicit blockers, next owner and work not performed. Product correctness, handoff validity and Temple administration remain separate observations. Historical v1/v2 reports and scores stay immutable.
- Collect fixed command categories, unknown/mixed coverage and observed UTF-8 output/request bytes. No raw prompts, outputs, paths or reasoning in additional telemetry; bytes are not Tokens. No always-on observer or model call is needed.
- Exercise the installed recipe locally, negative blocker cases, old protocol rejection, guard preservation and privacy/coverage controls. Run the full behavioral verification suite on the final candidate.

## Design and risks

Update first-party `project-overlay` Skill/template sources and human operational guides. Do not directly edit installed checksum-managed copies. Keep `context enter` optional: compact resolve plus required routed reads can be followed directly by `work-item finish`. Finish already runs Status/Doctor; repeat only after changed state or failed diagnostics. Do not add another executor or Position.

The self-host upgrade preview contains exactly the two changed Skill files, the handoff template, the previously implemented source `TEMPLE.md`, and `temple.lock`. Apply the supported upgrade to these checksum-clean files; no project-owned record replacement. The source contract has been read before adopting its installed copy. This is installation of existing source behavior, not another contract rewrite.

Use a new repository-only experiment contract and observation helper. The next frozen protocol must bind the changed instructions, schema and instrument; this change cannot consume an old approval. An unavailable output is unknown, not zero. Ambiguous shell commands are unknown/mixed, not evidence of permission or actual file reads. Candidate acceptance still requires the independent product oracle and actual tests; downstream declarations cannot hide real blockers.

## Overlap coordination

WI-0190 and WI-0211 are at Release Gate with released claims; their broad source/test scopes are retained history. WI-0234 is at Test with a released Developer claim and exact candidate `87466e5d298efee0479e0499fddba8ce7aecaef8`. This follow-on branch owns only the declared refinement paths; it does not rewrite their artifacts, acceptance or tested revisions. No parallel source edits are dispatched. Separate active worktrees and the existing PR stack remain untouched.

## Rollback and evidence boundary

Revert this candidate's source/doc changes if qualification fails. Keep the prior frozen experiment and its negative result. Offline command/byte reductions qualify structure only; measured Token, speed or quality improvement requires a separately authorized matched live comparison.
