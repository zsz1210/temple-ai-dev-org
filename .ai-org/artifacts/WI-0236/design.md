# WI-0236 — Reviewed integration and minimum follow-on comparison

## User authority

The user requested completing the proposed three steps: independent review, public-safe PR/integration, and a minimum real comparison. This supersedes the previous offline-only stop for follow-on work, but does not authorize npm publishing, a release, Credits purchase, refill, reset, fallback or repeated attempts. Existing default model/routing policy is not changed.

## Acceptance and execution order

1. Join WI-0235 review by Agent Identity `agent-lulu`, distinct from Developer `agent-rikku`, on candidate `68be154276fe2648a35eadf6c3e59c665ced2ea9`. Fix material findings before a new model run. Parent work may inspect publication records concurrently, but must not change the candidate source during review.
2. Preserve the existing public history/PR stack. Normalize supported released private coordinates and current artifact paths through the pinned CLI. Prepare an outgoing branch without publishing private intermediate ancestors. Retain original experiments and scores; any non-semantic normalization must be explicitly recorded. Inspect changed source/test fixtures and all outgoing commits for new private content. Run required checks, submit a focused PR, and integrate only after applicable review/CI gates pass. No admin bypass or policy change is implied.
3. Run exactly four fresh Terra medium Build subjects: one stable-requirement pair (ordinary, Temple) and one changed-requirement pair (Temple, ordinary). Same task/model/oracle/reporting semantics within each pair. Limit each to 100000 Operational Tokens and eight minutes; aggregate ceiling 400000 Operational Tokens and forty minutes. These are hard operational safety ceilings, not statistical thresholds or financial units. The current user request authorizes this bounded minimum experiment; old digest-bound approvals are not reused.

Use subscription authentication only; stop if unavailable. No retry, replacement, fallback, reset, purchase or refill. Freeze a new protocol/digest after native generation-free qualification and source verification. The first scheduled subject is the native-tool canary, not an extra call. If a shared validity failure or missing accounting occurs, stop the batch and preserve the partial result; do not silently repair and restart it.

## Measurements and interpretation

Report product oracle, blocker/owner reporting, Temple administration, full token components, operational Tokens, measured turn/harness time, native execution, command categories, observed byte coverage and cleanup separately. New reporting semantics apply to both arms. Old v2 outcomes remain historical and are not rescored. Four subjects support diagnosis only: no statistical efficiency claim, cache-control claim or automatic model-policy tuning.

The practical question is whether the model uses the reduced procedure and where any remaining overhead appears. If repeated work remains, report that result before proposing more samples. Do not add a harder scenario to rescue a negative result.

## Coordination and stop

The integration write inventory additionally includes `test/delivery-observations.test.mjs`, `.ai-org/artifacts/WI-0192/measurement.test.mjs`, `.ai-org/artifacts/WI-0193/measurement.test.mjs`, and `.ai-org/artifacts/WI-0211/verification.md`: replace synthetic home-path usernames with the already supported `fixture` username, preserving privacy-test assertions and historical outcomes. Supported canonical normalization may clear WI-0234's released claim coordinate. These are public-representation changes, not a change to experiment scores or the scanner's policy. This explicit inventory supplements the parent item's initial runner/test/artifact scope; no other source expansion is authorized.

The public PR is stacked on the existing PR #77 branch while main's code-owner/last-push approval gate is pending. No administrator bypass is authorized. The comparison may execute the locally reviewed, exact verified candidate before GitHub integration; it must be labeled unmerged and must not claim to test main.

WI-0235 is this item's review child. WI-0190, WI-0211 and WI-0234 have released claims; their accepted/tested source revisions are preserved. Stage shared instrument writes only after joining WI-0235. Publication normalization is confined to the documented existing normalization operations and explicitly reviewed synthetic-fixture representations. No concurrent product edit is authorized here.

Stop after the reviewed integration and this one sealed comparison/report, or at a genuine approval/validity boundary. Retain local evidence and original private branches; do not delete someone else's worktree, branch, test or running process.
