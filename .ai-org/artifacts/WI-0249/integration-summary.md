# Integration summary

Accepted behavioral candidate: `360b1b86266692aadda590b18419d3c3f004c352`.
Full offline suite: **1,279/1,279**, exit 0. Focused Developer run: **60/60**.
Independent Test: six selected cases and one separate functional-failure harness.
Independent QA: PASS with a separate hidden executable-mode harness. These are
separate measurements, not counts to add into an inflated unique-test total.

The Integration Owner joined the exact-candidate evidence and completed Standard
Test, Eval, Independent QA and the organizational Release Gate. WI-0249 is done;
both workers are completed and all its claims released. The all-active parallel
plan was refreshed after closeout. Final Doctor: **37 passed, 0 warnings,
0 failures**. Product code remains unchanged since the full verification run.

The bounded defect is corrected: pre-existing Git-hidden changed or missing
product cannot be newly handed off or accepted by ordinary finish. Direct deliver
and recovery share the same check. Existing journal/evidence/identity constraints
remain. A mutation after lifecycle writes still rejects settlement without a
fictional rollback; historical receipts do not become fresh verification.

The operator guide records these limits. Raw checkout transformations and unsafe
entries remain unsupported/fail-closed. No browser change, live model experiment,
merge, release, npm publication or deployment is included. Final evidence/prose
checks and the PR's remote CI are separate from the local full-suite result.
