# Context reading-scope clarification

The maintainer authorized implementing the bounded improvement and proceeding
directly to an experiment after offline checks and independent review. This is
not permission to merge, publish, buy Credits, reset usage or repeat failed runs.

## Finding and one intervention

`contextAuthorityPaths` explicitly describes its list as freshness inputs, not
instructions to load every body. The emitted compact `read_policy` does not make
that distinction explicit. An Agent can therefore mistake the authority snapshot
for a mandatory reading checklist. This is an interface ambiguity demonstrated
by source inspection, not proof that WI-0242's opaque commands actually did so.

Change only that existing read-policy string: read current scope, acceptance,
candidate evidence and required instructions; the measured authority inventory
is not a blanket read assignment. Explicit native/project, bootstrap, recovery,
selected and unselected reading obligations still take precedence. Missing or
unclear authority still requires resolution. Do not remove any source, hash,
reference, validation, claim, test, evidence, distinct verifier or gate.

No new Skill, Position, summarizer, routing mode, context schema, privacy policy
or model is introduced. Output gets a little longer; the hypothesis is fewer
unnecessary actions, not byte compression. This is an experiment candidate,
not a globally validated best practice or an automatic Learning promotion.

## Offline acceptance

Test the emitted CLI guidance and exact preservation of snapshot references,
scope, candidate, ownership, gate requirements and warning behavior. Verify
byte-identical non-policy compact output using parent/candidate pure functions.
Retain existing drift, missing-source, wrong-owner and recovery protections.
Run the full suite once on the final behavioral candidate, then distinct QA.
Reuse the accepted runner's isolation, usage and record-alias checks and run fresh
generation-free installed-schema, thread, oracle and native-observation preflight.

## Approved incremental experiment and stop

Use the existing two-subject incremental runner: stable then changed-spec,
Terra medium, 100,000 Operational Tokens/eight minutes per subject, 200,000/twenty
minutes aggregate. These inherited limits are stop ceilings, not predictions or
financial guarantees. Only Pro subscription authentication; no Credits, refill,
reset, retry, fallback, replacement or extra judge. Current account inspection
shows no reached limit; remaining percentage is not guaranteed Token capacity.

Freeze a fresh protocol and source/runtime/seed hashes after this change. Record
the digest under this current authorization before execution; do not reuse a
retired approval or resume old labs. Distinct QA must accept the exact candidate
and readiness first. Shared validity, missing usage, uncertain cleanup, source
drift or exhausted limits stop dispatch. Preserve failures as outcomes.

Reuse WI-0240's inventory and WI-0242's original sealed result as historical
references, never contemporary controls or freshly accepted samples. Only the
compact reading-guidance text changes in the distributed runtime relative to
WI-0244; the earlier alias-oracle repair and unknown-reason counters are already
present in both source baselines. No old solution enters the initial seeds.

Report both conditions including not-run rows, original acceptance, 46-case
product results, administration, uncached/cached/output/Operational Tokens,
elapsed time and command/unknown-reason counts. Byte counts cannot prove read
behavior or Token causes; historical differences cannot prove causal savings.
There is no pass threshold on cost: retain or reject the hypothesis from actual
quality and directional evidence, with no automated policy change. Stop after
the reviewed report and PR. Execution evidence will use a separate Work Item.
