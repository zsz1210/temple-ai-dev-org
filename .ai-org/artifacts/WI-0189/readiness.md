# WI-0189 generation-free readiness

The explicit approval in approval.md covers the unchanged seven Terra medium stages, 600,000 Operational Tokens and 45-minute live matrix. No subject generation occurred during these checks.

- Six experiment-specific tests passed: topology/cap sum, cumulative parallel usage, immutable/cross-module scope, semantic shell policy, exact-revision join, and installed-wire synthetic success/timeout/cap/cancellation.
- Real App Server command sandbox: seed public tests failed; reference public tests and held-out checks passed; replacing each of the three reference modules with the seed stub failed the oracle.
- An actual attempt to write outside the actor sandbox was denied.
- Installed Provider schemas accepted all seven request shapes. Terra medium was offered. Memories are disabled; managed ChatGPT authentication was observed. No login, purchase, refill or reset was performed.
- Frozen protocol digest: sha256:0283881373ffbf51fa2d82d9146fc898e128f9ab38e4453d52a32e8a2c99eb8c.

This is developer readiness, not formal Independent QA or a delivery result. Raw check output and machine paths remain in the private lab. Preparation was revised before any run-once marker or subject turn; no subject was retried. The first generation-free staging directory is not an experiment result.

The adapter retains the tested first-party runner's notification identity checks, installed schemas, model acknowledgement, semantic command checks, per-stage timers and Token caps. Local changes add module write scopes, common prompts, cumulative cross-worker accounting and cancellation. Three worker repositories are isolated; the coordinator joins only each committed owned module/test pair. Product rejection may continue the independent arm; protocol, accounting, scope, time or Token failures stop all activity without resumption.

Limitations: n=1, fixed A-then-B order, cache uncontrolled, Provider usage is last-observed rather than guaranteed billing-final. Parent planning/inference Tokens are unavailable and must be reported as unattributed. Full wall-time comparison includes integration and verification; it does not include the initial one-off harness development.
