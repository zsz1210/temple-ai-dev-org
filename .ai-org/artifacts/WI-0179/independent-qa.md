# WI-0179 independent readiness QA

Verdict: **PASS for generation-free readiness** at exact candidate `175f7ada3821b6e57e5b176f5e7af6624144abec`.

The candidate is safe to freeze as the live comparison protocol. This is not authority to run it. Fresh matched results remain pending, and model generation remains blocked until the maintainer explicitly approves the exact frozen matrix, aggregate limits, requested routes and no-retry/no-fallback account policy.

## Identity and exact scope

Independent QA ran on 2026-09-05 as `agent-lulu` (Lulu), assigned to `independent_qa`. The assigned Developer is the distinct Identity `agent-rikku` (Rikku). The active runtime was `worker-20260905092412-f4907318`, claim `claim-20260905092412-493ece82`, runtime `/root/wi0179_comparison_qa`, on branch `codex/wi-0179-optimized-comparison` in `<LOCAL_HOME>/Documents/ChatGPT/temple-wi-0179-optimized-comparison`.

The pinned compact Context preview resolved WI-0179 at `independent_qa`, exact Developer candidate `175f7ada3821b6e57e5b176f5e7af6624144abec`, and selection digest `sha256:fc9857f36f0504f5321e654ad6fd66cc0db49793f293e08645d71a932a1e6768`. QA changed no source, canonical state, approval, lifecycle, other worktree or historical artifact. This report is the only QA-owned repository write.

Environment: Darwin arm64, Node `v24.20.0`, installed `codex-cli 0.153.1`. The project uses the Standard workflow and standard risk tier; this is not High-Assurance work.

## Exact-candidate evidence

- Independent recomputation returned source digest `sha256:d31aff9ed119e2532d8f4ccc00d0ada1f6e2f76d676602f022c5badb7fe56e96` and process contract `delivery-process/v6`, digest `sha256:96977af8fa19b6aa1e79d31ff9a60415165b4ec98d871362e765565df3abdd69`.
- `git diff --exit-code 0002dacce9152918f00906f4ae611e06e5c153af..175f7ada3821b6e57e5b176f5e7af6624144abec -- src/codex-app-server-provider.mjs src/app-server-protocol-replay.mjs scripts/run-representative-microservice-comparison.mjs` passed. Current and parent provider SHA-256 both equal `0e8649e4f4cd948b8396777c6227be02ff5d7c15694db0ae3c1b6b78189b79e3`; the owned-process shutdown behavior was not regressed by this candidate.
- `npm run verify` passed repository/package/link checks and **568/568 tests**, zero failures, in 98.421 seconds. The previously reported optional Console timeout did not reproduce. No Console source or timeout was changed.
- Independent installed-sandbox replay, `TEMPLE_DELIVERY_SANDBOX_REPORT=<temporary-path> node --test --test-name-pattern='delivery pair readiness and actual injected lifecycles' test/delivery-control-pair.test.mjs`, passed 2/2 reported tests in 11.241 seconds. It completed all four injected Builder/Verifier stages, observed 81 operations, matched all started/completed commands, passed supplied/added/held-out product checks, exercised compact Context and composed delivery, and denied both attempted outside writes. It made zero Provider thread or turn requests and no model generation.
- Generation-free installed-provider probes confirmed both `gpt-5.6-terra / medium` and `gpt-6-astra / medium`, disabled memories, and no model generation. Provider contract digests were `sha256:32f85aacbaef3c649d2c805637ad6777b92921713971eefaee89f294c03363d3` and `sha256:ea8665b86d533e2a06ba31900d7fceaf9fb11b4a04154770386205b86c851baa`, respectively.
- `node ./templew.mjs doctor . --json` passed **37**, warned 0, failed 0. Read-only Status still showed WI-0179 at `independent_qa` with the exact candidate and active Lulu claim.

## Independent challenges

1. **Matrix order and aggregate bounds — pass.** The full plan has four pairs/16 stages, 1,280,000 maximum Operational Tokens and 5,760,000 ms; the Terra-only plan has two pairs/eight stages, 640,000 Tokens and 2,880,000 ms. Each selected model has one ordinary-first and one Temple-first pair. The matrix digest binds selection, order, models, effort, per-pair limits, aggregate limits and policy; each pair binding pins its protocol and Provider contract.
2. **Whole-matrix stop and one-shot behavior — pass.** QA prepared a disposable Terra matrix with test-only readiness and forced the matrix wall-clock preguard after its exclusive lock. The run stopped as `matrix-wall-clock-limit`, retained `matrix-run.json` and `matrix-run-once.lock`, started zero pairs/turns and created no pair run artifacts. A second invocation failed `EEXIST`; it could not resume or repeat the stopped matrix. This was generation-free and the disposable lab was removed.
3. **Protocol, source and readiness binding — pass.** Run inspection revalidates every frozen protocol/Provider digest, exact route/order/limits, current source digest, fixture, command policy, output schema, process contract, clean fresh arm state and Independent QA/sandbox evidence before a live Provider process starts. Source, route, budget, fixture, Provider and readiness drift cases fail before subject generation.
4. **Treatment versus product quality — pass.** Correct product output alone is insufficient. Temple Build requires successful compact Context, a non-dry-run composed delivery and verified exact lifecycle/handoff state; Temple Verify requires successful compact Context. `efficiency_comparable` additionally requires all four stages, product/oracle success, valid workflow state, treatment adherence and complete observed usage. Preview, failed command, prompt text or output volume cannot stand in for actual treatment or Tokens.
5. **Ordinary-control fairness — pass within the frozen task.** Against historical runner `bba20cc140b72068827c7c858008c9768a16f067`, the fixture digest remained `sha256:ce39cdfb577d8d276dd82b43149e17623d923703eef133015c3a2c97780b3e0b`. QA independently hashed the complete ordinary Build and Verify requests in both worktrees and obtained the same values on each side (`a86dd6f88783d3b64a2ae61daf5c3a8bb699495503b56ce4465ebcf123f0f192` and `ff20e156055c538b4d53149c978f7a9c46ce2df2a5b01ff2c3dd73eeb219b4d9`). Ordinary retains Git, full tests, exact revision evidence, concise handoff and a fresh verifier; it is not weakened to make Temple win.
6. **Product/evidence mutation boundary — pass.** Product scope is only `order.mjs` and `test/added.test.mjs`. Root `DELIVERY.json`, `HANDOFF.md`, `VERIFICATION.json` and Temple observer artifacts are allowed post-commit evidence, while exact candidate blobs for implementation, public tests and added tests must match Git. Public fixture changes, verifier product writes, symlinks, Git configuration/hooks drift and writes outside the active repository fail closed. The actual installed sandbox denied two practical outside-write attempts.
7. **Sealed-history isolation — pass.** New matrix roots must be outside and disjoint from the source and must not already exist. Every pair refuses pre-existing run/lock/seal artifacts and seals its retained run/artifact digests. The WI-0173 historical worktree remained clean when independently inspected; QA made no write there. Historical outcomes remain context only, never a causal control for this changed harness.

## Explicit limitation and live gate

`validateMatrixApproval` cryptographically binds an approval document to the entire matrix and exact aggregate policy, but it only requires nonempty `approved_by` and `evidence_ref` strings. A practical counterexample using `approved_by: "not-a-verified-principal"` and `evidence_ref: "does-not-exist"` passed structural validation. The runner therefore cannot authenticate human intent or prove that a referenced approval exists; the coordinator must verify the real maintainer approval artifact before invoking `run`. This is a hard operational precondition, not a readiness failure in the current scope: the harness creates no approval, current approval is explicitly pending, and no generation was attempted.

Installed route availability can drift after this check. A live stage's effective effort may remain unreported and must stay unknown; requested model/effort and model-list support are not proof of effective inference routing. Provider usage is last-observed and not final account billing. The installed-sandbox replay used injected deterministic actor behavior even though command execution and write denial were real. It proves the execution boundary and lifecycle mechanics, not future model correctness, latency, Tokens, statistical significance, financial savings or Temple superiority.

No efficiency claim is supported before the approved matched matrix completes. Freeze the exact protocol only after binding this report and the existing sandbox evidence; then stop pending explicit aggregate approval. No retry, fallback, additional pair, routing-policy change, PR, merge, release or publication is authorized by this QA pass.
