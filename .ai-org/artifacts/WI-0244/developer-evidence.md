# Publication preparation evidence

Developer agent-rikku; exact candidate 1a98048da0990c04c0cb9b02ffa5d25ad778d013.
Full release verification is running and must pass before publication.

- Package rebuilt with 444 files, 997361 bytes, SHA-256
  03c725189ace782c362b7deee65854980d4072a22600983ed290ec1c652138d2, identical
  to WI-0243's qualified archive. No packaged path or behavioral test changed.
- Text publication blockers: zero. The scanner correctly still requests manual
  binary review; binary-review.json supplies exact records for all 110 items.
- 68 screenshots exactly match WI-0160's prior reviewed digests. The new screenshot
  was viewed: only synthetic Work Item states/public framework labels, no private
  values or embedded text/EXIF chunks. QA must make its independent visual judgment.
- All 41 archives expanded in memory with bounded sizes and safe member types;
  no member was executed. Twelve copies required normalization: 2766 repeated home
  paths, four email literals and 47 AppleDouble records, with owner metadata removed.
  Current expanded-text scan has zero unresolved matches. Before/after archive and
  member hashes are retained; original history and original measurement numbers remain.
- The normalized archive paths and three corrected text files have no references
  in the active normalized Evidence registry. Doctor remains 36 pass, two expected
  warnings (old actor policy and plan freshness), zero fail before final closeout.
- Historical helper import now resolves repository-relatively; syntax check passes.
  Two already-redacted placeholder mentions are reworded, not two secret exposures.

Distinct QA must compare original Git bytes to public copies, classify the binary
review boundary, verify exact archive identity and inspect full test completion.
User approval for Alpha.33 prerelease/npm next is recorded in plan.md. It does not
permit a different archive, latest changes, real downstream upgrades or policy bypass.
