# Approved incremental compact experiment

The maintainer's current request to start testing approves the immediately
preceding two-subject proposal, including independent QA before dispatch. It is
fresh authority for the exact digest below, not reuse of a retired approval.

Protocol: `continuity-incremental-approved/v1`.
Digest: `sha256:3568e1f6ec0bf33665d0275661829a9c10c8a0ccde4b3f2fc4dfa453d2333dc2`.
Prepared lab identifier: `temple-continuity-live-ftTGJ4` (coordinates remain local).
Instrument candidate: `afb048d10a7c1554afa75b8121d733d753b0a49b`.
Read [readiness](../WI-0241/readiness.md) and [design](../WI-0241/design.md) for
the frozen fixtures, controls and historical compatibility limits.

## Scope and ceilings

- Exactly two fresh compact Temple Developer subjects, stable then changed-spec,
  using `gpt-5.6-terra` / `medium` and the existing runner once.
- At most 100,000 Operational Tokens and eight minutes per subject; at most
  200,000 and twenty minutes for the batch. These are stop ceilings, not forecasts.
- Pro subscription authentication only. No Credits purchase/refill, reset,
  replacement, retry, fallback, model substitution or additional judge subject.
- Preserve completed product failures as outcomes under the frozen typed policy.
  Unknown usage, uncertain cleanup, source drift, invalid observation or exhausted
  limits stops further dispatch. No repair, restart or continued tuning in this run.
- The proposed automatic Learning loop is not installed or tested here. Actor
  instructions and runtime are the previously accepted compact treatment.

## Current preflight and decision

Read-only checks on 2026-09-07 confirmed exact protocol/envelope/instrument/runtime
digests, both clean actor repositories at their frozen initial commits, no
consumed/run/seal file, and matching Codex CLI 0.153.4. The current account reports
Pro with 59% of its weekly window used and no reached limit; Credits balance is
zero. Percent remaining is not convertible to an assured Token allowance.

The independent reviewer must accept WI-0241 before this execution enters Build.
The reviewer does not generate model subjects or modify the frozen lab. All old
studies, protocols and outcomes remain unchanged; ordinary and previous Temple are
historical references, not freshly executed controls or pooled measurements.

Only WI-0242 evidence/state and the approved isolated actor execution roots may
change during the run. WI-0211 is a released predecessor and has no active writer;
WI-0241's review is separate and joined before dispatch. Rikku owns execution,
Lulu evaluates and independently reviews evidence, and Mog coordinates.

Acceptance is an honest sealed outcome and condition-by-condition report, including
unattempted cells if stopped. Report correctness, administration, input/cache/output/
Operational Tokens, setup and elapsed time, failures and limitations separately.
Stop after this report and review; no merge, release, npm publication or next
experiment is inferred. Cleanup uses the frozen runner; retain evidence, not a
destructive rollback of the lab.
