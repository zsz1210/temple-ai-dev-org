# WI-0195: Repaired native support comparison

Run the same four sealed support arms as WI-0193 against the reviewed WI-0194 event recorder. The first arm is again the live compatibility gate; a mandatory stop retains evidence and prevents later arms. The experiment compares the previously selected before and after Temple revisions, not WI-0194 itself.

Use `gpt-5.6-terra` at `medium` effort for every parent and the one informational helper allowed per arm. Maximums are eight subject turns, 100,000 Operational Tokens per actor, 800,000 observed aggregate Operational Tokens, eight minutes per actor, and 40 minutes overall. These are safety ceilings, not expected usage, prices, statistical power, or evidence that parent and child usage is nonduplicated.

All fixtures are disposable local repositories. Product files are read-only by instruction and verified by outcome; the native workspace sandbox does not enforce that narrower boundary. No network, package installation, external tracker mutation, release, deployment, retry, fallback, credit purchase, automatic top-up, or automatic reset is permitted.

Before execution, freeze the protocol, executable bindings, installed provider contract, fixture snapshots, request digests, and sandbox probes. A distinct reviewer must accept the exact candidate and seal. Live execution additionally requires a fresh human approval bound to that seal and numerical envelope; WI-0193's spent zero-retry approval is invalid here.

Report every attempted arm, stop reason, parent/helper lifecycle evidence, cleanup uncertainty, answer quality, token observations and elapsed time. Do not call a stopped or unmeasurable arm a failure of Temple. Do not claim efficiency, model superiority, statistical significance, or complete live compatibility from this four-arm diagnostic.

