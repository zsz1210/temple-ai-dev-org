# B/C delivery cost measurement

The user authorized design followed by measurement once readiness passes. This is a fresh bounded request, not reuse of a consumed approval. Measure one ordinary/Temple pair for Terra medium and one for GPT-6 Astra medium, with fresh Builder and Verifier sessions: eight actor stages, at most 80,000 observed operational Tokens and six minutes per stage, 640,000 Tokens and 48 minutes aggregate. Existing included allowance only; no purchase, refill, reset, extra actor retry or fallback. Check availability before generation. Missing effective effort, billing and immutable model release remain unknown.

Pin Temple product behavior to accepted B/C base 7c9c1b88b96b8d334c5c0a88bd287f473ce4fc42. Changes are experiment instrumentation only. WI-0190 onward and historical labs/approvals remain untouched. The ordinary and Temple arms use identical shipping-quote requirements, public tests, held-out oracle and fresh exact-candidate verification. Terra is ordinary-first and Astra Temple-first; order and caches are descriptive confounders, not controlled estimates. One pair per model is diagnostic, not statistical reliability or model ranking. Prior v7 numbers are historical context only.

The treatment deliberately selects context enter and work-item finish. Native required instructions still apply. A body delivered through the entry packet can be reused only when actually available and unchanged. A passing finish must carry current successful diagnostics, not a dry-run, historical receipt or lifecycle-only success. Product correctness, fresh verification, treatment adherence, and resource cost are separate outcomes.

Measure per-stage operational/cached/total Tokens and wall time. Classify observed command counts, returned bytes and execution duration into context acquisition, product tests, Git, lifecycle administration and other; patch events are implementation activity. Never allocate model inference Tokens to command categories by bytes or elapsed-time proportions. Repeated command hashes are repeats, not proof of redundant same-state work. Report unknown/unmatched events rather than zero. Separate setup and evaluator costs from actor time, and disclose excluded coordinator inference and harness development.

Before generation: exercise all four synthetic stages through the installed provider command sandbox, test identity/revision/evidence and preview/diagnostics counterexamples, reconcile category totals, run full verification and obtain distinct-Identity readiness QA on the exact source. Freeze protocol/source/approval bindings, then execute once. Stop on protocol/provider/safety/budget interruption or a noncomparable pair; retain partial observations without automatic retry. Finish after the bounded comparison and evidence report, without merging or publishing.

## Source and ownership

WI-0203 was uniquely created by the coordinating task through its pinned CLI on 2026-09-06 and copied byte-for-byte from that registry into this isolated branch. The original allocation has no claim; all subsequent ownership is recorded here. Additional likely editing paths discovered during intake are scripts/delivery-control-pair.mjs, scripts/delivery-command-policy.mjs and their test files, plus test/optimized-delivery-comparison.test.mjs. Existing configure does not update affected_paths; record this scope here without hand-editing canonical JSON. Shared script changes occur only on this branch and were communicated to the other task. No change to Temple core behavior, installed instructions or usage policy.

Acceptance requires a tested instrument, sealed new evidence, truthful incomplete outcomes when applicable, and a concise result separating quality from cost. Full reports are retained locally for later analysis.
