# Test setup performance work order

The maintainer authorized the next recommendation after WI-0234: measure and reduce repeated repository initialization and CLI harness overhead, retaining all distinct test scenarios and safety assertions. Baseline is the accepted pruning closeout `e4ea52ac`, whose unchanged behavioral candidate `220eda6180c1806d4bc261c9332bf3e55ed92a0b` passed 1237 tests in 288684.186083 ms.

Scope: test-only setup and subprocess harness optimization, starting with `test/delivery-control-pair.test.mjs`, plus bounded profiling and evidence. Do not change production source, live experiment contracts, test discovery, timeouts, assertions, policy, dependencies or model routing. Preserve the installed CLI and shell integration paths, independent fixture state, negative cases, failure injection, cleanup and exact operation evidence. No merge, publication, deployment, model generation or account polling.

First capture an instrumented focused baseline, separating fixture copy/preparation, process counts and elapsed time. Select the smallest justified optimization from those observations. Reuse only immutable initial state; never reuse mutated repositories, completed results or test verdicts. Preserve real CLI wiring where it is the actual subject. Keep model/sandbox prerequisite behavior unchanged.

Acceptance: all existing scenario names/outcomes remain; any new helper has independent isolation/failure controls as needed; baseline/candidate instrumentation reports comparable workloads and setup/process counts; the final committed behavioral candidate passes one complete `npm run verify`; distinct Agent Identity independent review accepts coverage and measurement claims; Doctor and evidence-only fast verification complete closeout. Developer is agent-rikku, reviewer agent-lulu, Principal human. Sequential implementation; governed reviewer dispatch is prepared only at the final candidate.

Measurement budget: one focused instrumented baseline, focused correction runs only when needed, up to three focused candidate timing samples if measurement noise warrants them, and one complete final verification. Do not rerun the whole baseline merely for timing. Treat a single full-suite before/after as diagnostic, not controlled performance proof. Preserve raw observations and disclose instrumentation or environmental limitations. Stop after a verified local optimization and a concrete next recommendation.

## Measured implementation choice

The first instrumented focused baseline passed all 35 results in 57754.851459 ms on Node v24.20.0. It initialized five delivery-pair templates despite using only two exact arm orders. Preparation invoked init five times, work-item setup ten times, transition five times and Doctor five times; these 25 Temple CLI preparations took 9644.701126 ms. Actor CLI, Git, shell and product checks are the actual integration subject and remain real executions.

Cache the pristine prepared pair by exact arm order inside this one test, then copy it for every scenario. At most two preparations are needed. Preserve ordinary and Temple initial Git histories, manifest/order/digests, all actor ledger operations, source/fixture-drift checks and per-test directory cleanup. Add one isolation control for product bytes, canonical lifecycle bytes and Git configuration in both orders. This choice reduces redundant initialization without changing production or introducing a shared mutable repository, a persistent cross-run cache, or a reused test verdict.

The exploratory baseline/candidate runs exposed random initial arm-order confounding (actor Temple-context calls were 13 versus 15). Their timings cannot isolate the optimization. Correct the measurement method with one additional controlled baseline/candidate pair, both forcing Temple-first only through the temporary profiler; production randomness remains unchanged. Execute the exact baseline test body from a temporary non-discovered file under `test/`, using the same unchanged production modules, source directory, Node runtime and profiler. Remove that temporary file before committing or full discovery. This bounded correction adds one baseline run, not a repeated full suite or another product scope.
