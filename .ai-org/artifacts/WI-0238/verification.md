# Developer verification and Test/Eval evidence

Historical rejected attempt. Independent QA subsequently found F1–F3 in
`independent-qa.md`; the passing tests below did not prove obligation preservation.
Its readiness digest is retired. Corrected evidence must name a new candidate.

Candidate: `173f43e1becdca8c3f1e672b173497b95441cd71`.
Developer: Rikku (`agent-rikku`). Date: 2026-09-07 UTC.

## Final complete check

`npm run verify`, Node.js **24.20.0**, exit **0**:

- Repository and documentation link checks passed.
- Actual npm package boundary passed: 416 files, 893,838 packed bytes and
  3,503,460 unpacked bytes. The one added distributable file is ADR-0062; no package
  exclusion or size ceiling was relaxed.
- **767 tests passed, 0 failed**, Node-reported duration **183,915.620625 ms**.
- No model generation, UI/browser work, publishing or deployment occurred.

The suite covers installation/upgrade, project-owned preservation, unknown/missing
whole authority, identity/profile exceptions, bootstrap and recovery, real claim
and finish, exact product/record commits, oracle and negative controls, provider
replay, budgets/cancellation, current three-treatment matrix and runtime manifests.
These are offline tests, not model-following or measured efficiency evidence.

Targeted editing checks also passed: 33 continuity/operating checks (before the
final documentation-contract correction); 17 Skill/context/support checks and
5 installation/bootstrap checks. They are not added to the 767 count or presented
as an additional full run. Final full verification supersedes them.

## Preserved development failures

Earlier checks stopped on project-facing brand wording and the new ADR exceeding
the reviewed package count. Wording was corrected; the package ceiling increased
by exactly the reviewed ADR. Instruction-contract tests also depended on the old
duplicated paragraph locations. The first complete `f7587a0f` attempt had 766/767
passing and one such bootstrap-location failure. Tests now follow the retained
whole contract, without removing real bootstrap/import/ownership checks. Exact
CLI suggested-title and unreadable-authority obligations are explicit. No failed
candidate is presented as final passing evidence.

The optional system Skill Python validator could not import PyYAML in either
available Python environment. It is **not claimed as passed**. Repository-native
Skill frontmatter, references, scenarios and full installed behavior passed; no
new dependency was installed for the auxiliary validator.

## Generation-free comparison qualification

[Readiness](readiness-summary.json) and [proposal](comparison-proposal.md) retain
the six-subject frozen protocol and body-free measurements. Current and previous
runtime configuration checks passed, with confirmed provider exits; current/stale
oracle controls, real record-only delivery and native observation qualification
passed. Both Temple variants pass the same 46-case oracle after real claim/finish.
No `turn/start`, consumed approval, model Tokens, Credits purchase or reset.

Preparation uses standalone Node.js 24.19.0. The ordinary Homebrew runtime was
correctly rejected for external dynamic-library dependencies; permissions were
not widened. A temporary exclusive-copy mistake was corrected and now has a
regression check. Preparations made before final instruction edits are obsolete;
only digest `5291b060d39a21618adbb48de9c31da489b306655fea693a96fcfb45adc3b5e8`
is retained as the current unconsumed proposal.

That preparation pins `f7587a0f22b5c182cb5e5d175253cae92ec60c97`. Git comparison
against the final `173f43e1` confirms byte-identical runtime code, scripts,
dependencies, distributed/installed instructions and lock. Only a test's old
location assertion and its design note changed afterward. Qualification is reused
for identical behavior, not presented as a new provider run.

## Test/Eval assessment

The approved local treatment is present and materially smaller. Four whole sources
remain four whole sources: 27,200 to 15,408 UTF-8 bytes, down 43.35%. On the matched
changed-spec fixture the selected whole bodies are 74,588 to 62,796 bytes, down
15.81%; current product facts remain exactly 1,400 bytes. Navigation is unchanged.
Serialized entry size is reported separately. No Token/time benefit is inferred.

The [obligation map](obligation-map.md) covers read-only, Lean, Standard,
High-Assurance, UI, trackers, bootstrap, scope drift, uncertainty and parallel
ownership. No new Position, workflow, command, automatic cache or approval bypass
was added. Distinct formal Independent QA remains required before closeout.
Remaining external decision: authorize the new diagnostic, not repeat old samples.
