# Matched instruction comparison

Prepared candidate: `af5a984aee6706ab308c693d95e138134162d5d6`.
Frozen protocol: `continuity-instructions-approved/v1`,
`sha256:dafc7fa80cdb274770c8bb339df2bb74a5067f615ce783c02856e80b5804cd27`.
Status: generation-free qualification passed; no live execution or consumed approval.
The former `5291b060` preparation is retired after the failed QA and instruction
correction, not reused as permission or final evidence.
See [readiness summary](readiness-summary.json) for provenance and measured bytes.

## Question and controls

Does removing repeated operating prose reduce small-task delivery overhead without
losing correctness, scoped authority, current-spec recovery or honest handoff?
This tests the Developer delivery stage, not an entire enterprise organization,
all Positions or all project sizes. Coordinator preparation and subsequent QA
are separate costs, not silently included in the actor's Token measurement.

| Order | Current requirement | Treatment |
| --- | --- | --- |
| 1 | Stable | Competent ordinary handoff |
| 2 | Stable | Previous Temple instructions |
| 3 | Stable | Compact Temple instructions |
| 4 | Changed specification | Compact Temple instructions |
| 5 | Changed specification | Previous Temple instructions |
| 6 | Changed specification | Competent ordinary handoff |

Each state is created from one shared product/history/specification seed. Ordinary
receives the same approved requirements, prior evidence and handoff facts, without
Temple administration. The two Temple actors receive the same request, Identity,
profile, executable code, acceptance and isolated runtime. Only four distribution
instruction files differ; previous bytes are pinned to WI-0237's `4c606f71`.
Both install through the real CLI; neither edits installed managed files/locks to
fake the treatment. Generated timestamps/checksums remain actual installation data.
The current evaluator accepts a tested product commit followed by valid record-only
delivery, while still rejecting dirty, missing, forged or out-of-scope changes.

## Measurements and decisions

Report every attempted subject separately, including a failed or stopped attempt:

- Product oracle (46 cases), regression execution and protected-source validity.
- Temple administration and honest next-owner handoff, distinct from product pass.
- Input, cached input, output and Operational Tokens; preserve unknown acquisition.
- Turn/complete-attempt elapsed time, command counts and available byte observations.
- Blockers, interruptions, rework and any unobserved component.

Compare old/new Temple within each requirement state, then contrast each with
ordinary. Never pool historical WI-0234/WI-0236 samples or reinterpret their failures
under this new evaluator. Actual source acquisition is observed, not inferred from
the smaller on-disk files. No command reduction is credited for the already-existing
one-finish/diagnostics reuse mechanism. No extra mandatory entry JSON call is added.

Cache cannot be forcibly cleared here. Preserve actual cache ratios and order;
unequal cache or runtime conditions limit causal interpretation. There is one
subject per state/treatment, no replication or randomized population inference.
An authority or acceptance regression rejects adoption. Otherwise report quality,
Tokens and elapsed time separately and use the complete attempts to decide whether
this simpler contract is worth retaining. There is no invented minimum saving,
automatic router/policy promotion or claim that a byte reduction predicts Tokens.

## Proposed execution limits, not permission

All six candidates use `gpt-5.6-terra`, `medium`, matching the existing fixed-model
diagnostic. Retain its 100,000 Operational Tokens and eight-minute per-subject
stop. Six subjects give a proposed 600,000-token batch ceiling; sixty minutes retain
the prior allowance of two minutes per subject for qualification/cleanup overhead.
These are stop limits inherited from a bounded harness, not predicted usage,
statistical sample requirements or financial spending guarantees.

Pro-included usage only. Zero retries, zero fallback, no Credits purchase, no
auto-refill or reset. Unknown usage, source/configuration drift, uncertain cleanup
or an invalid instrument stops the sequence; a valid product failure is retained
as a result under the existing typed continuation rule. No replacement subjects.
Only fresh approval of this exact frozen digest can authorize execution. Earlier
approvals and earlier preparation hashes are not reused.

## Reproduction

Run the committed `prepare.mjs` in this directory with a Node.js 24+ standalone
runtime whose libraries fit the existing sandbox. It creates an exclusive private
lab, checks current provider configuration, installs the three treatments, executes
synthetic oracle/record controls and freezes an unconsumed protocol. It never sends
`turn/start`. Repreparation creates a new digest and is not permission to run it.
Private roots, provider schemas and runtime configuration stay in the lab; only
body-free summaries are retained in Git. Recheck source/bundle/fixture fingerprints
at launch; do not modify a frozen lab or relax permissions to make it run.
