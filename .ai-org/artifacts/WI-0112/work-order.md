# WI-0112 work order

Run the frozen Wave 5A Temple-versus-minimal feasibility comparison after the merged WI-0111 quote-aware command-policy correction and a fresh exact-schema preflight pass.

The run is bounded to four sequential GPT-5.6 Luna Max turns, uses a new isolated `r4` lab, permits no retries or fallback, and stops the entire program on the first invalid or incomplete attempt.
