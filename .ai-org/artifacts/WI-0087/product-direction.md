# WI-0087 Product Direction

The supported Node.js matrix is part of Temple's public contract. A release candidate is not ready when one supported hosted lane fails, even when the failure occurs after the behavioral assertions.

This correction is limited to reliable disposal of test-owned temporary directories. It must not change inbox behavior, authorization logic, network boundaries, or user-facing output.

Acceptance remains the three criteria recorded in `.ai-org/work-items/WI-0087.json`.
