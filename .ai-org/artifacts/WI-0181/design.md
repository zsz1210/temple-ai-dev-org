# WI-0181 compact diagnostics and stage completion

Authorized by the user's request to complete the other task's remaining work and take over delivery-efficiency improvements. WI-0179 is concluded/inconclusive; its source, labs and consumed approvals stay frozen. This Standard bounded slice changes optional CLI projections and an existing maintainer-owned Skill. It grants no release, integration or model experiment authority.

## Contract and acceptance

- Doctor always executes the same complete checks. `--compact` returns the same healthy flag, counts and exit status; only pass messages are omitted. All non-pass checks remain verbatim. JSON uses a distinct schema, counts omitted passing checks and labels full validation explicitly. Text mode also preserves every warning and failure.
- Status always builds its full projection and, unless `--no-write`, writes the same full generated views. `--compact --json` reduces only stdout to project totals, full global attention and selected item projection. With `--work-item WI-ID`, the full existing projected row for exactly that item is retained; without it, no individual rows are emitted. Unknown IDs and unsupported flag combinations fail before view writes. It is navigation/observation, not acceptance or Doctor health.
- Compact output must not silently truncate warnings, failures or attention; large exceptional output is intentional. Defaults remain backward compatible. No policy, lifecycle guard, validation coverage or framework-managed ownership changes.
- Update the existing source Skill's finish clause: complete the assigned stage and required checks, return exact handoff/state/unresolved information, and do not continue later responsibilities solely to claim whole-work completion. Doctor after canonical changes remains mandatory. Compact checks are optional supported output modes, not shortcuts around validation. Changes are original project prose; trigger, dependencies and authority remain unchanged. Update installed managed copy through upgrade, never manual lock edits.
- Validate runtime healthy/warning/failure cases, missing installation, scoped/global attention, no-write/default-write behavior, invalid options, schema distinction and unchanged ordinary output. Full local suite and distinct-Identity Independent QA are required. Byte deltas may be measured but are not inferred Token, latency or quality gains. No live forward model test under consumed approval.

## Risk review and stop condition

Main risks are concealing failures in a compact view, mistaking stage handoff for project acceptance, and mutating frozen test protocols. Preserve exceptional detail and default behavior; keep historical harness unchanged. This slice stops at tested local candidate, evidence report and follow-up test design. Per-operation Token accounting and altered matrix-stop policy are separate future changes.

Rollback is a Git revert of this additive candidate; no state migration or data deletion is introduced. No graphical interface changes; CLI observation presentation only. The receiving coordinator continues local governance and delivery, then an independent QA runtime reviews the exact candidate.
