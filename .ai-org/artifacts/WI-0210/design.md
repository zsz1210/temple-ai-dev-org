# Complete the frozen three-arm comparison

## Authority and outcome

The user explicitly requested repair followed by the complete comparison. Run a new exclusive lab under WI-0210, preserving WI-0208/0209 attempts. Retain twelve fresh gpt-5.6-terra / medium actor stages: ordinary/prior/slim/slim/prior/ordinary, Builder then Verifier. Retain 80,000 observed Operational Tokens/six minutes per stage and 960,000/72 minutes aggregate. Pro included allowance only; no purchase, refill, reset, retry, fallback or additional judge. Stop at a genuine safety, provider, budget or noncomparable outcome rather than manufacture success. No release/merge authority.

Freeze Temple product code and treatment prompts. Change only the common experimental command grammar, attempt identity and focused regression coverage before generating the protocol. Prior source stays 87d68a189d03df33a61df753f441acdb387c7835; slim product functionality is unchanged from the reviewed WI-0209 source. Do not tune product code while collecting measurements.

## Narrow compatibility repair

The previous observed command was classified as read-only git-diff with argument-shape rejection; its exact text is unknown. Independently reproduced legitimate counterexamples include `git diff --` and `git diff --stat --`. Accept an empty trailing separator for existing allowlisted Git read operations only, with existing prefix option/revision restrictions intact. Nonempty suffixes still require safe local paths. Git add/commit/rev-parse retain their current specialized rules. Reject repeated separators, outside paths, pathspec magic, arbitrary revisions, external diff drivers, output flags and shell composition.

Use installed Git to check semantic equivalence, not only a mock sharing the parser assumption. Test build and verify contexts, direct and zsh envelopes, ordinary and Temple arms. Keep actual event validation, Git safety and OS containment; the observation policy is not a pre-execution firewall.

## Readiness and freeze

Run focused negative controls, prior regression suites and npm run verify. Independent QA must review the exact candidate. Create fresh pinned source/configuration/fixture/request/provider bindings and replay six exact trust additions without mutating user configuration. Probe both slim sandboxes with no model generation, including outside-write denial. Verify canonical lab-path aliases and immutable finalization error reporting before launch. Bind a new one-shot approval and independent review to the new protocol; never reuse consumed approval or rewrite old seals.

Record all outcomes, observed usage/cache, actor time, context volume, tests and rework. A stopped delivery is not a cheap success. Two repetitions are diagnostic, not statistical proof. Final report must distinguish complete comparison from partial data and identify improvements supported by the data.

## Coordination

WI-0209 is an explicitly handed-off predecessor with no active claim; its remaining command-compatibility finding is assigned to this follow-up. WI-0172 is an inherited intake reservation without active ownership. No parallel writes or edits to the separate cost-measurement worktree are planned. Existing independent QA may review read-only; source ownership is Rikku.

## Sources

Git's documented diff syntax has independently optional separator and path operands: https://git-scm.com/docs/git-diff . Codex command/exec supports generation-free sandbox checks and config/read exposes effective layered configuration: https://learn.chatgpt.com/docs/app-server . Validate against installed schemas and Git behavior as well as documentation.
