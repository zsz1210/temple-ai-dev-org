# WI-0110 work order

Run the previously designed Wave 5A Temple-versus-minimal feasibility comparison only after the merged WI-0109 offline replay gate and a fresh exact-schema preflight pass.

The run is bounded to four sequential GPT-5.6 Luna Max turns, uses a new isolated lab, permits no retries or fallback, and stops the entire program on the first invalid or incomplete attempt.
