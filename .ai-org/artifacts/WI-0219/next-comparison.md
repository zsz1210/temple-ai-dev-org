# Next comparison: decision-ready, not authorized to run

## Evidence-based decision

The supplied literal context-entry commands passed twelve local real CLI
executions across both roles, both formats, direct/wrapped shell forms and
omitted reuse. Sixteen synthetic malformed argument cases were rejected with
specific diagnostics. No supported-command incompatibility was reproduced.
Do not change production acceptance, invent a helper or advertise a root-cause
fix. WI-0217's exact failed command remains unknowable from retained evidence.

## Proposed bounded live comparison

- One fresh counterbalanced sequence: Full, Model, Model, Full; Builder and fresh
  distinct Verifier per delivery, eight stages total.
- Same task, candidate source, model (Terra medium), prompts and acceptance per
  format. Retain the current transport; only diagnostic visibility changed since
  WI-0217. Pin the new classifier and exact source into a new protocol.
- Retain 640000 operational Tokens aggregate, 48 minutes aggregate, six minutes
  per stage, 80000 stage warning. These are inherited authority boundaries, not
  optimal budget estimates. Subscription allowance only; no purchase, refill,
  reset, retries, fallback or extra judge.
- Complete fresh sandbox/source/provider checks and independent readiness,
  consume one explicit approval, stop on the first existing failure boundary.
  Do not promise completion; model-authored command variation can still stop it.
- If a command fails, retain the new bounded diagnostic, count its costs and
  interrupted outcome, and report what is and is not known. Do not repair the
  command and resume the same run or discard a failed sample.

## Report requirements

Show all scheduled and attempted deliveries, product/acceptance results, actor
elapsed time, total/cached/non-cached input and output, operational Tokens,
within-turn rework and guard failures. Report complete-pair descriptive results
separately from reliability and the cost of incomplete attempts. Do not pool the
stopped previous runs as if they were this counterbalanced sequence.

Two deliveries per format still cannot establish general superiority, routing
authority or a stable failure rate. The comparison asks whether the smaller
representation has a useful signal on this fixture, not whether Temple is
universally more efficient. No new model turn is authorized by this document.
