# Local context command compatibility

Run the actual generated format-comparison entry command through the experiment
classifier and the installed repository launcher, in direct and one-layer shell
forms. Cover Full/Model and Builder/Verifier using a disposable, lifecycle-valid
fixture. Read the whole sources before declaring reuse; verify unchanged bodies,
matching declarations and canonical read-only behavior. Also test omitted reuse.

Synthetic malformed JSON, invalid rows, missing value and extra positional cases
must produce the new privacy-safe diagnostics. These are reproduction classes,
not recovery of WI-0217's unretained historical command. Fix production behavior
only if a valid supported command reproducibly fails. Do not relax the guard to
force completion, invent read acknowledgement, add a wrapper or rerun live models.

Complete focused and full checks, distinct-Identity review and a concise next
experiment design. New live spending/turns require a fresh bound approval; prior
sealed labs remain untouched. Tests and evidence are the only planned writes.
