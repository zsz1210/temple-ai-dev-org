# Approved format-only experiment

The user approved the preceding explicit envelope: four deliveries/eight stages, gpt-5.6-terra medium, at most 640000 operational Tokens and 48 minutes, subscription allowance only, no purchase/refill/reset/retry/fallback. This record grants the bounded run only after offline checks, exact-source/request/provider bindings, real sandbox preflight and distinct-Identity readiness review. The final immutable protocol digest is bound to this approved envelope; no old approval or consumed run is reused.

Implement a narrow successor wrapper sharing the existing fixture, isolation, provider schema inspection, event policies, quality oracle and evidence seal. Each arm uses identical task material and process-v9 wording; the only request difference is `--format full` versus `--format model`. Sequence: full, model, model, full. Per-stage stop ceilings remain 80000 operational Tokens and six minutes. Stop on the first failed quality/guard, missing usage, uncertain termination or ceiling; do not retry to improve results. Keep requested/observed effort, cached input and incomplete usage explicit. No statistical or pricing inference from two samples per arm.

Extend shared observation/policy only behind an explicit expected context-format option. Existing harness behavior and old frozen laboratories must remain unchanged. Model schema recognition requires the new opt-in, and the requested format must be enforced by the command guard. Tests must reject wrong/missing format, stale bindings, duplicate consumption and incompatible request/response treatment. Before live execution, freeze source and fixtures; validate requests against installed provider schemas, verify tool isolation, probe both formats without a model turn and deny outside-lab writes.

Acceptance: full repository verification passes; distinct agent-lulu approves exact harness/readiness; run terminates within the envelope, seals every outcome including a stopped run, and produces stage/delivery quality, Tokens, latency, context output, reread/test counts and limitations. A stopped run is not a completed comparison. Rollback the wrapper and opt-in additions; no product core behavior changes or release.

Overlap coordination: WI-0172, WI-0208, WI-0209, WI-0210, WI-0212 contain prior benchmark source/evidence and remain frozen with no concurrent source edits. This successor modifies only the explicitly listed harness paths from the current verified branch. WI-0190 and WI-0211 broad test/src records remain retained; exact new test file and shared benchmark scripts are successor-owned. No old lab is changed. WI-0214 is the coordinating parent.
