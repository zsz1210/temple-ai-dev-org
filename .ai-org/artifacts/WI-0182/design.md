# WI-0182 fresh v7 screening comparison

The user explicitly requested another comparison as the next step after WI-0181 (translated: "Then do another comparison, that is the next step."). This is a new run request, not reuse of WI-0179's consumed approval. Retain the previously requested model categories, `gpt-5.6-terra` medium and `gpt-6-astra` medium. No purchase, reset, refill, fallback or retry is authorized.

One new ordinary/Temple pair per model gives eight fresh actor stages. Terra is ordinary-first; GPT-6 is Temple-first. Each stage retains the previous 80,000 observed Operational Token and six-minute cap; aggregate caps are 640,000 Tokens and 48 minutes. This reduces the previous 16-stage matrix scope and does not raise its per-stage limits. The assistant disclosed these bounds before execution. Source, protocol and approval bindings must be recorded after local rehearsal and independent review and before any generation. Existing allowance was available at intake; account limits are not Token-to-credit conversion evidence.

## Design and acceptance

- Keep product task, public tests, held-out oracle, memory isolation, fresh Builder/Verifier, exact candidate acceptance, no-retry and stop-on-noncomparable rules unchanged.
- New process v7 explicitly ends both arms after their assigned stage, evidence and required checks. Temple must execute compact scoped Status and compact Doctor after its final lifecycle mutation. Preserve the requirement to rebuild full Status views; permit both compact write and no-write variants in the observer grammar. Noncompact reads remain valid but cannot substitute for the compact treatment.
- Recognize compact diagnostics as distinct operations and require successful post-mutation observations for treatment adherence; a compact check before deliver/transition is insufficient. Product correctness and treatment adherence remain separate results.
- Parameterize matrix Work Item binding, add an eight-stage screening selection, and keep existing plan selections and their default WI binding compatible. Reject wrong IDs, limits, option combinations, extra scope and stale approvals before provider generation.
- Actual installed-provider sandbox rehearsal uses command/exec only, no model turns; it exercises all four synthetic stages and forbidden write boundaries. Full offline verification plus distinct-Identity independent readiness review precede matrix preparation and live execution.
- Report requested/acknowledged model, requested/observed effort and unobserved effective effort, quality, elapsed stage time, operational/cached/total Tokens, diagnostic output bytes, and all incomplete or censored stages. No causal inference from bytes, one sample, or comparisons to historical runs.

## Authority, risk and bounded completion

The current user request authorizes this fresh bounded comparison within existing allowance, including the necessary local harness adaptation. It does not authorize more turns after a stop, a higher cap, new models, account resets, merge or release. The approval record will cite this actual request and exact frozen matrix, not claim that the user inspected hashes or reuse an older approval.

WI-0172 survives as an intake record in this checkout but its comparison work is isolated in the historical bba20cc branch. WI-0179 is concluded and its original worktree, source, protocols, labs, locks and results remain untouched. New code is isolated on codex/wi-0182-comparison-v7. No concurrent owner is editing these paths here. No visual UI change or lifecycle policy change.

Keep the existing stop rules: safety/provider/budget interruption stops its pair; a quality failure ends that arm, and a noncomparable pair stops the matrix. Otherwise stop after eight completed stages. Seal and report either outcome. Do not change the experiment after seeing results. Rollback is a source commit revert for the local harness, never deletion or resealing of evidence.
