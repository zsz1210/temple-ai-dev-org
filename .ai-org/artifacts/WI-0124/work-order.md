# Work order — WI-0124

Seal the remaining Execution Request projection boundaries: restrict explicit generation timestamps to the managed Route date-time subset, enforce semantic unknown-property allowlists at every Request object boundary, and project only declared Route fields.

All prior QA failures remain evidence. Provider execution, model selection changes, external actions, push, merge, deployment, publication, and release are excluded.
