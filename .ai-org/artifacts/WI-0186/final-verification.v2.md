# WI-0186 corrected local verification

Behavioral candidate: `3cf56d80b27f5499f65b2357d8c3602c30cdbfbf`.

`npm run verify` passed repository, link and package checks and **589/589 tests**, zero failures/skips, in **108.733 seconds**. Full log SHA-256: `f4d1ee131a83c3e252c94b88c78a6deb0440bf6eaf8e31ad6ad32903ef77e8b9`. After rebuilding the runtime plan, Doctor passed **37/37**, zero warnings/failures. Implementation, tests, scripts and documentation remained unchanged against the candidate. After organizational closeout, `npm run verify:fast` passed repository/link/package checks, Doctor and **54/54 fast tests**; the fast-test runner took 1.067 seconds. Final changes contain evidence and lifecycle state only.

The first candidate's `independent-qa.md` is a preserved **failed** review. The correction restores independently required whole-source reasons from selected Context routes and approved repository specifications. Two new CLI regressions pin both forms to both structured authority sources, assert exact full bodies and unchanged canonical bytes, and passed within the full suite. Fresh `independent-qa.v2.md` records **PASS** on the corrected candidate: 18 independent packet tests plus additional integration-purpose and technical-contract probes passed. QA Identity `agent-lulu` differs from Developer `agent-rikku`. The first full verification does not qualify this repaired source.

The corrected candidate reproduced committed `measurement.json` byte-for-byte using `measure.mjs`. It compares the same synthetic stage snapshot with full and optional stage acquisition, without model calls:

| Stage | Full body bytes | Stage body bytes | Full CLI output bytes | Stage CLI output bytes | CLI reduction |
| --- | ---: | ---: | ---: | ---: | ---: |
| Developer | 71,293 | 54,352 | 90,861 | 80,975 | 10.88% |
| Fresh Verifier | 73,508 | 57,218 | 94,754 | 86,376 | 8.84% |

These are single-fixture byte observations against the previous full packet, not ordinary delivery. Dynamic synthetic evidence can vary slightly between executions. Full local source acquisition remains unchanged. Whole governing policies, entry documents, procedures, handoff and evidence remain intact; no required read is waived. Projected output explicitly distinguishes original sources from emitted bodies and binds full-source freshness.

No live model inference was used in this measurement, so the measurement's model and reasoning effort are not applicable. Developer and reviewer agents are not benchmark participants. No Token, price, model-latency, quality-superiority or general delivery-efficiency result is established.

Publication audit reports zero blocked files and the same 68 preexisting binary files requiring review; it is not publication approval. This bounded local slice adds an opt-in presentation mode. Merge, publication, new live comparisons and later implementation slices remain outside this closeout.
