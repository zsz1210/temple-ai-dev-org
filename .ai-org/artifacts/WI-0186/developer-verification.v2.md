# WI-0186 corrected Developer evidence

Candidate: `3cf56d80b27f5499f65b2357d8c3602c30cdbfbf`. This attempt supersedes the first candidate only after fresh verification and Independent QA pass. The original failed review remains in `independent-qa.md`.

The repair restores independent Context-route and approved repository-specification selection reasons that compact Context separates from authority source paths. A source explicitly required by either reference therefore retains its entire body in stage mode. Whole acquisition and freshness checks still run before projection.

Two actual CLI regressions pin valid Context routes and approved content-hashed repository specifications to both `temple.lock` and `.ai-org/core/positions.json`. They assert complete acquisition, independent reasons, whole-source fallback, exact original bodies and unchanged canonical bytes.

Focused packet tests: 18 passed, 0 failed or skipped, 23.042 seconds. Full `npm run verify` is running on this corrected candidate; its final result must be recorded separately before acceptance. The earlier 587-test full run does not qualify the repaired source.

Scope remains the accepted brief and ADR-0056. No live model comparison, Token/latency inference, new provider choice, external action or publication is included.
