# WI-0184 authorized slice A

The user approved continuing after the WI-0183 design. Implement only its first slice: opt-in, transient acquisition of stage material. This is an acquisition foundation, not an approved shortcut around required instructions, a workflow/profile change, or a live efficiency trial.

Scope: a read-only `context packet` command for current low-risk bounded Lean Build/Test, source provenance and deduplication, explicit incomplete/fallback responses, freshness binding, additive docs and tests. Keep the existing Context schemas and default behavior unchanged. No new model generation, source summary, lifecycle mutation, model switch, external action or frozen comparison edit.

Acceptance:

1. Derive scope, stage, ownership, candidate and source selection from the existing resolver. Deliver whole relevant local files once with hashes, reasons and byte counts. Skills discovered only as suggestions stay supporting references.
2. Preserve mandatory AGENTS.md, TEMPLE.md and applicable temple-work references. Include recorded gate and handoff sources; unresolved external/Evidence references remain explicit blockers rather than invented evidence.
3. Bound acquisition, reject unsafe/non-text/oversized input, detect drift and never return a complete packet on missing or changed inputs. Distinguish acquired selected files from semantic coverage of every instruction.
4. No body persistence, claim, authority grant or mutation plan. Existing validators still govern all later operations. Cold sessions gain no permission to skip instructions.
5. Exercise genuine CLI and fixture behavior, no-write snapshots, safety, missing/stale sources, provenance, deduplication and compatibility. Run full verification and independent exact-candidate review.

Risk: local source content is returned only through explicit opt-in stdout to the current caller. No external transmission or new retention backend; existing caller/provider policy applies. Do not add `.git` or external files to source acquisition. Revert additive source/docs to roll back; preserve prior evidence. Default entry instructions are unchanged.

Prior WI-0183 remains an intake design record on the ancestor branch, with no overlapping write scope. WI-0182 remains sealed. No parallel source owner is editing this branch. Engineering Manager integrates; Developer implements and distinct Independent QA reviews the candidate. Accepted local slice does not imply public release or measured savings.
