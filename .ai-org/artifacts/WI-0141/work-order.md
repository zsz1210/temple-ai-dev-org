# WI-0141 work order

## Outcome

Run the live comparison prepared by WI-0140 and determine whether stage-aware Context Capsules change post-route source acquisition, correctness, Operational Token use, or latency for single-repository and coordinator-led multi-repository recovery.

## Authorized slice

- promote the accepted WI-0140 generation-disabled design into a new WI-0141 protocol and artifact boundary;
- keep the WI-0140 artifact subtree unchanged and verify it before protocol freeze and immediately before generation;
- prepare eight fresh disposable conditions: two repetitions of each strategy for each repository shape, in the accepted counterbalanced order;
- request Terra with medium reasoning for every condition;
- retain only the bounded structured completion, numeric usage, timing, tool-activity summary, and path-only acquisition metadata allowed by the protocol;
- after exact approval, execute each condition at most once and produce a per-shape comparison report.

## Stop conditions

Before approval, stop after generation-free preparation, rehearsal, tests, and preflight have produced a frozen protocol digest and an unapproved approval template.

After approval, stop after the first complete run or the first run-level failure. A condition reaching its declared Token ceiling may be retained as censored, but it is never retried. Do not use fallback, purchase Credits, enable automatic refill, consume a reset, merge, publish, deploy, or grant automatic model-routing authority.
