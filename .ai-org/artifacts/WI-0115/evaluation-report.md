# Evaluation report — WI-0115

The deterministic acceptance criteria pass at candidate `11012a38523676f1187fbea8b4a388ba4d81bb18`:

- a locally packed clean consumer completed offline install, init, installed-launcher Doctor, and read-only Status;
- the bootstrap contract named and permitted deterministic reads of all three canonical sources;
- the Claude adapter contained only `@AGENTS.md` and remained project-owned;
- all bootstrap authority flags remained false;
- provider loading, comprehension, intervention, and Token evidence remained explicitly not run or `null`.

The outcome qualifies only the deterministic onboarding path. A fresh provider session is a separate, spend-governed experiment and is not required to accept this bounded runner or its truthfulness boundary.
