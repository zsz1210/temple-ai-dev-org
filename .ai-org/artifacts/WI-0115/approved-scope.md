# Approved scope — WI-0115

The user approved a clean greenfield onboarding validation followed by offline Wave 5A analysis and a later qualified Wave 5B comparison. This Work Item covers only the first slice: package a current local candidate, install it in a disposable Git repository, initialize from an explicit synthetic configuration, run the installed launcher, and retain truthful timing and authority-boundary evidence.

Provider-owned model execution is not silently authorized by this scope. If no separately approved bounded provider run occurs, model loading, comprehension, and Token usage remain `unknown` rather than being inferred from deterministic files.
