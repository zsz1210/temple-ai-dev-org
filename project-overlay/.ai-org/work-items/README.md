# Work items

Store one canonical JSON file per work item, named by durable ID such as `WI-0001.json`. Create it with `temple work-item create`; use `temple transition` for state changes and named gate evidence. Titles may change; IDs must not.
