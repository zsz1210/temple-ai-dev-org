# Uninitialized sample product

This deliberately contains no Temple Agent identities. Tests copy this small repository shape to a temporary directory, supply project-specific names at runtime, and prove that Phase 1 can initialize an existing project without overwriting its files.
